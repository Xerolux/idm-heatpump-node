---
phase: 02-modbus-reads-detection-and-resilience
plan: "09"
type: execute
wave: 7
depends_on:
  - 02-01
  - 02-07
  - 02-08
files_modified:
  - contracts/api-mapping.json
  - contracts/typescript-extensions.json
  - docs/API-PARITY.md
  - src/client/index.ts
  - src/index.ts
  - test/parity/api-parity.test.ts
autonomous: true
requirements:
  - TRN-01
  - TRN-02
  - TRN-03R
  - DET-01
  - DET-02
  - ERR-01R
must_haves:
  truths:
    - "Four fully owned Phase-2 Python mappings become complete only after focused evidence."
    - "IdmModbusClient becomes an exact release-blocking partial with 22 implemented Phase-2 members and seven Phase-3 omissions."
    - "ModbusTransport becomes a complete type-only root extension while Python mapping remains 89/89."
    - "Root runtime/type exports equal authorized complete mappings, the reviewed partial class, and complete extensions with no internal adapter/dependency seam leakage."
    - "Generated API documentation and declarations expose only the exact mapped constructor and members."
  artifacts:
    - path: "contracts/api-mapping.json"
      provides: "Four complete rows plus exact partial IdmModbusClient lifecycle"
    - path: "contracts/typescript-extensions.json"
      provides: "Complete additive ModbusTransport type"
    - path: "src/index.ts"
      provides: "Explicit evidence-gated Phase-2 root surface"
    - path: "test/parity/api-parity.test.ts"
      provides: "Status/member/constructor/export/declaration closure evidence"
    - path: "docs/API-PARITY.md"
      provides: "Generated truthful complete/partial/additive projection"
  key_links:
    - from: "contracts/api-mapping.json"
      to: "src/index.ts"
      via: "explicit mapping-authorized exports"
      pattern: "IdmModbusClient|IdmClientDiagnostics|IllegalAddressError|ModbusErrorContext|quietPymodbusLogging"
    - from: "contracts/typescript-extensions.json"
      to: "src/index.ts"
      via: "type-only additive export"
      pattern: "ModbusTransport"
---

<objective>
Promote the exact mapped and additive Phase-2 public surface under G-01/G-02 without claiming Phase-3 or release completion.

Purpose: Make the real read/detection client usable from the private package while retaining exact Python inventory, constructor/member truth, and release blocking.
Output: Mapping/extension status updates, generated API projection, explicit client/root exports, and closure tests.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@contracts/api-mapping.json
@contracts/typescript-extensions.json
@docs/API-PARITY.md
@scripts/generate-api-parity.mjs
@src/client/idm-modbus-client.ts
@src/client/index.ts
@src/index.ts
@src/transport/types.ts
@test/fixtures/public-classes.json
@test/parity/api-parity.test.ts
@.planning/phases/02-modbus-reads-detection-and-resilience/02-01-SUMMARY.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-07-SUMMARY.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-08-SUMMARY.md

<interfaces>
The exact Phase-2 implemented IdmModbusClient member set is:
`clearLastErrorContext`, `connect`, `decodeValue`, `detectModel`, `disconnect`, `encodeValue`, `forceReconnect`, `getBatchUnsafeRegisters`, `getDiagnostics`, `getLastErrorContext`, `getUnsupportedRegisters`, `host`, `isConnected`, `markBatchUnsafe`, `modelInfo`, `modelName`, `port`, `probeRegister`, `readBatch`, `readRegister`, `readValue`, `resetFailedRegisters`.

The exact Phase-3 omissions are:
`getActiveCyclicWrites`, `getExpiredCyclicWrites`, `resetCyclicWriteState`, `resetWriteThrottle`, `setValue`, `simulateWrite`, `writeRegister`.

The public constructor has required host plus options containing only port, slaveId, timeout, maxRetries, and maxGroupSize.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-09-01: Promote exact complete, partial, and additive authorities</name>
  <files>contracts/api-mapping.json, contracts/typescript-extensions.json, docs/API-PARITY.md, test/parity/api-parity.test.ts</files>
  <read_first>
    - contracts/api-mapping.json
    - contracts/typescript-extensions.json
    - test/fixtures/public-classes.json
    - scripts/generate-api-parity.mjs
    - test/parity/api-parity.test.ts
    - all Phase-2 focused test files and summaries
  </read_first>
  <behavior>
    - IdmClientDiagnostics, IllegalAddressError, ModbusErrorContext, and quietPymodbusLogging become complete with exact focused evidence.
    - IdmModbusClient becomes partial/release-blocking with exactly the 22 implemented and seven omitted camelCase members above.
    - The implemented/omitted lists are duplicate-free, disjoint, and equal the pinned class fixture union.
    - The mapped constructor records only the reviewed options-object and internalized-pymodbus-retry normalizations.
    - ModbusTransport becomes complete type-only through the extension authority.
    - Python rows remain exactly 89 and release validation remains red.
  </behavior>
  <action>
Write RED promotion tests for statuses, exact member partition, constructor option closure, no public IllegalAddressError.code, order-preserving diagnostics factory, logging aliases, and extension evidence. Run all focused Phase-2 evidence, then update only eligible rows/extension. Keep IdmModbusClient partial and never fabricate not_applicable or complete status. Regenerate `docs/API-PARITY.md`.
  </action>
  <verify>
    <automated>npm test -- test/parity/api-parity.test.ts test/client test/transport/modbus-serial-adapter.test.ts &amp;&amp; npm run parity:api</automated>
  </verify>
  <acceptance_criteria>
    - Exactly four Phase-2 Python rows are complete and the client row is exact partial.
    - The 22/7 partition and constructor normalization pass fixture-derived validation.
    - ModbusTransport is separately complete and Python inventory remains 89/89.
    - Release validation still rejects the partial class.
  </acceptance_criteria>
  <done>Every Phase-2 public status is truthful, evidenced, and machine-closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-09-02: Expose only the authorized root surface and declarations</name>
  <files>src/client/index.ts, src/index.ts, test/parity/api-parity.test.ts, docs/API-PARITY.md</files>
  <read_first>
    - src/client/idm-modbus-client.ts
    - src/client/diagnostics.ts
    - src/client/internal-create.ts
    - src/transport/types.ts
    - src/transport/errors.ts
    - src/transport/logging.ts
    - src/transport/modbus-serial-adapter.ts
    - src/client/index.ts
    - src/index.ts
    - test/parity/api-parity.test.ts
  </read_first>
  <behavior>
    - Root runtime exports add exactly IdmModbusClient, IdmClientDiagnostics, IllegalAddressError, ModbusErrorContext, and quietPymodbusLogging.
    - Root type exports add exactly ModbusTransport from the complete extension authority.
    - Internal creation factory/token/dependencies, adapter class/factory, normalized failures, logging hook registration, and parser helpers remain absent.
    - Built ESM/CJS declarations expose only the mapped public constructor and exact 22 implemented members; no seven omitted write members or stubs exist.
    - parity:api regenerates documentation and parity:check validates generated evidence non-mutatingly.
  </behavior>
  <action>
Write RED runtime/type export closure and emitted-declaration tests. Use explicit named `.js` exports—never `export *`. Promote the five mapped runtime symbols and type-only extension. Scan source and built declarations for internal dependency/adapter names and omitted Phase-3 members. Run `parity:api`, then non-mutating `parity:check`, then build.
  </action>
  <verify>
    <automated>npm test -- test/parity/api-parity.test.ts -t "root|export|declaration|constructor|member" &amp;&amp; npm run parity:api &amp;&amp; npm run parity:check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Root runtime/type keys equal the authorized mapping and extension projection.
    - No adapter, internal dependency seam, raw failure, or omitted write member leaks.
    - ESM, CJS, and declarations build successfully from one implementation.
    - Generated docs/evidence have no drift after check.
  </acceptance_criteria>
  <done>Private consumers can import the real Phase-2 client and transport type without false write or release parity.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------- |
| Mapping/evidence → public package       | Status, members, constructor, and exports must not overclaim Python parity. |
| Internal modules → emitted declarations | Adapter and injection seams must remain unreachable from consumers.         |

## STRIDE Threat Register

| Threat ID | Category               | Component                        | Disposition | Mitigation Plan                                                     |
| --------- | ---------------------- | -------------------------------- | ----------- | ------------------------------------------------------------------- |
| T-02-06   | Tampering              | mapping/extension/member closure | mitigate    | Exact 89 rows, 22/7 partition, generated projection, release block. |
| T-02-04   | Information disclosure | root/declarations                | mitigate    | Explicit exports and forbidden-symbol scans.                        |
| T-02-03   | Spoofing               | public error/status claims       | mitigate    | Focused evidence before promotion and no additive Error member.     |
| T-02-SC   | Tampering              | package supply chain             | accept      | No dependency installation occurs in this plan.                     |

No ASVS L1 high-severity item may remain open at plan completion.
</threat_model>

<artifacts_produced>

## Artifacts This Phase Produces

| Artifact                           | Purpose                                       |
| ---------------------------------- | --------------------------------------------- |
| four complete Phase-2 mappings     | Exact public data/error/helper parity         |
| exact partial client mapping       | Read/detection surface with Phase-3 omissions |
| complete ModbusTransport extension | Type-only additive public contract            |
| explicit root/declarations         | Authorized private-package public surface     |

</artifacts_produced>

<verification>
Run both focused commands, `npm run lint`, `npm run format:check`, `npm run typecheck`, `npm run parity:api`, `npm run parity:check`, and `npm run build`.
</verification>

<success_criteria>

- Public status/member/constructor evidence is exact and release-blocking where partial.
- Python mapping stays 89/89 and ModbusTransport stays separately additive.
- Root runtime/types and built declarations expose no internal or deferred symbol.
- The package remains private and no release completion is claimed.

</success_criteria>

<output>
Create `.planning/phases/02-modbus-reads-detection-and-resilience/02-09-SUMMARY.md` when done.
</output>
