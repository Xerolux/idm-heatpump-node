---
phase: 02-modbus-reads-detection-and-resilience
plan: "08"
type: execute
wave: 6
depends_on:
  - 02-07
files_modified:
  - package-lock.json
  - package.json
  - src/client/idm-modbus-client.ts
  - src/transport/modbus-serial-adapter.ts
  - test/transport/modbus-serial-adapter.test.ts
autonomous: true
requirements:
  - TRN-01
  - TRN-02
  - TRN-03R
  - ERR-01R
must_haves:
  truths:
    - "The audited exact modbus-serial 8.0.25 package is the only direct runtime dependency."
    - "Adapter setup, FC03/FC04, unit ID, normal/temporary timeout, exact words, numeric Code 2, error normalization, close, and destroy are tested without TCP."
    - "The adapter is fully hidden behind ModbusTransport and raw library types/errors never enter public declarations or diagnostics."
    - "The normal public IdmModbusClient constructor obtains the real adapter through internal default wiring while exposing no transport/time injection options."
  artifacts:
    - path: "src/transport/modbus-serial-adapter.ts"
      provides: "Real Modbus TCP provider behind ModbusTransport"
    - path: "test/transport/modbus-serial-adapter.test.ts"
      provides: "Mocked no-network adapter and default-wiring evidence"
    - path: "package.json"
      provides: "Exact single runtime protocol dependency"
    - path: "src/client/idm-modbus-client.ts"
      provides: "Internal default adapter binding for public construction"
  key_links:
    - from: "src/transport/modbus-serial-adapter.ts"
      to: "src/transport/types.ts"
      via: "direct adapter-neutral implementation"
      pattern: "ModbusTransport"
    - from: "src/client/idm-modbus-client.ts"
      to: "src/transport/modbus-serial-adapter.ts"
      via: "internal default factory only"
      pattern: "createModbusSerialTransport"
---

<objective>
Add the audited real Modbus provider and bind it to normal client construction required by D-01, D-02, D-04, D-05, D-08, and D-13.

Purpose: Make the completed read/detection client usable without allowing the concrete library to define domain requests, retries, errors, diagnostics, or public constructor shape.
Output: Exact dependency/lock, hidden adapter, mocked tests, and internal default-factory wiring.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@package.json
@package-lock.json
@src/transport/types.ts
@src/transport/errors.ts
@src/transport/logging.ts
@src/client/idm-modbus-client.ts
@src/client/internal-create.ts
@.planning/phases/02-modbus-reads-detection-and-resilience/02-RESEARCH.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-07-SUMMARY.md

<interfaces>
The selected dependency is exact `modbus-serial@8.0.25`. The adapter may use `connectTCP`, `setID`, `setTimeout`, `readHoldingRegisters`, `readInputRegisters`, `isOpen`, callback close, and destroy internally. Only numeric `modbusCode === 2` establishes illegal address.

The public constructor remains host plus mapped options only. Default adapter selection is internal implementation wiring; test transport/time injection continues only through `src/client/internal-create.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-08-01: Re-audit and install the exact runtime provider with a RED adapter contract</name>
  <files>package.json, package-lock.json, test/transport/modbus-serial-adapter.test.ts</files>
  <read_first>
    - package.json
    - package-lock.json
    - .planning/phases/02-modbus-reads-detection-and-resilience/02-RESEARCH.md Package Legitimacy Audit
    - official `modbus-serial` v8.0.25 `package.json`, `ModbusRTU.d.ts`, and tagged source cited by research
    - src/transport/types.ts
    - src/transport/errors.ts
  </read_first>
  <behavior>
    - Registry metadata confirms exact version 8.0.25, official repository, non-deprecated status, no install script, and expected integrity before installation.
    - A mismatch stops before package mutation.
    - Package files add exactly one direct runtime dependency at exact version and preserve private/runtime/build declarations.
    - RED mocked tests specify connectTCP, ID, seconds-to-milliseconds timeout, FC03/FC04, timeout restore, Code 2, other failures, exact words, close, and destroy without opening TCP.
  </behavior>
  <action>
Re-run `npm view` metadata and inspect the exact tagged package source before installation. Because research classified this exact package [OK], no human legitimacy checkpoint is required; stop on any mismatch. Install with `npm install --save-exact modbus-serial@8.0.25`. Write the mocked adapter contract tests first using an injected constructor/client boundary. Assert no second direct runtime dependency, install script, or public adapter type is introduced.
  </action>
  <verify>
    <automated>npm test -- test/transport/modbus-serial-adapter.test.ts &amp;&amp; npm ls modbus-serial --depth=0</automated>
  </verify>
  <acceptance_criteria>
    - Exact verified dependency and normal lock entry exist.
    - No other direct runtime dependency was added.
    - The adapter test suite is RED for the complete required boundary without any live network.
    - Package remains private.
  </acceptance_criteria>
  <done>The repository contains one verified exact runtime provider and an executable adapter contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-08-02: Implement the hidden adapter and internal default client wiring</name>
  <files>src/transport/modbus-serial-adapter.ts, src/client/idm-modbus-client.ts, test/transport/modbus-serial-adapter.test.ts</files>
  <read_first>
    - src/transport/types.ts
    - src/transport/errors.ts
    - src/transport/logging.ts
    - src/client/idm-modbus-client.ts
    - src/client/internal-create.ts
    - official `modbus-serial@8.0.25` declarations and tagged source
    - pinned `client.py` `_connect_internal` and `_read_registers`
  </read_first>
  <behavior>
    - Connect configures TCP host/port, normal timeout, and unit ID; one client attempt produces one library read call.
    - Holding maps to readHoldingRegisters/FC03 and input to readInputRegisters/FC04.
    - Per-request timeout sets adapter milliseconds and restores the normal timeout in finally after success, Code 2, socket failure, or invalid response.
    - Returned data is exactly count integer words 0..65535; numeric Code 2 creates IllegalAddressError; other errors use closed normalized kinds/redaction.
    - Close and destroy are idempotent Promise wrappers.
    - Normal public construction uses this default internally; declarations/API mapping still contain no factory/clock/sleep/adapter fields.
  </behavior>
  <action>
Implement the thinnest adapter that owns only connection, method selection, timeout switching, word extraction, and error normalization. Use the library for Modbus framing; do not hand-roll sockets or add retries. Bind `createModbusSerialTransport` as the client's internal default without changing the public overload or exporting the factory. Keep the internal direct-path test seam operational. Make all mocked tests GREEN and scan declarations for adapter/injection leakage.
  </action>
  <verify>
    <automated>npm test -- test/transport/modbus-serial-adapter.test.ts test/client/lifecycle.test.ts test/client/resilience.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm ls modbus-serial --depth=0</automated>
  </verify>
  <acceptance_criteria>
    - Adapter tests prove FC03/FC04, ID, timeout restore, one call/attempt, Code 2, error kinds, words, close, and destroy.
    - Client behavior remains identical through the fake internal seam and normal construction selects the real default.
    - Public declarations contain no `modbus-serial` or dependency injection type.
    - No socket is opened by tests.
  </acceptance_criteria>
  <done>The package has one exact hidden Modbus TCP provider whose incidental API cannot alter domain parity.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                                |
| -------------------------------------------- | -------------------------------------------------------------------------- |
| npm package → adapter                        | Third-party protocol code must be exact, audited, locked, and isolated.    |
| Adapter → client                             | Raw responses/errors/timeouts must become exact words and closed failures. |
| Internal default wiring → public constructor | Real provider selection must not expose injection or adapter fields.       |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                                     |
| --------- | ---------------------- | -------------------------- | ----------- | ----------------------------------------------------------------------------------- |
| T-02-SC   | Tampering              | modbus-serial supply chain | mitigate    | Exact version, official metadata/source recheck, lock integrity, no install script. |
| T-02-03   | Spoofing               | device exceptions          | mitigate    | Numeric modbusCode 2 only.                                                          |
| T-02-04   | Information disclosure | raw errors/declarations    | mitigate    | Closed redaction and no raw/provider type exposure.                                 |
| T-02-05   | Tampering              | response/method selection  | mitigate    | Exact function/type/count/word tests and client revalidation.                       |
| T-02-02   | Tampering / race       | timeout/lifecycle          | mitigate    | Client FIFO plus adapter finally restore and idempotent close/destroy.              |

No ASVS L1 high-severity item may remain open at plan completion.
</threat_model>

<artifacts_produced>

## Artifacts This Phase Produces

| Artifact                      | Purpose                                           |
| ----------------------------- | ------------------------------------------------- |
| exact runtime dependency/lock | Audited protocol provider                         |
| hidden modbus-serial adapter  | Real FC03/FC04 transport implementation           |
| mocked adapter tests          | No-network protocol/lifecycle/error proof         |
| internal default wiring       | Usable public constructor without injection drift |

</artifacts_produced>

<verification>
Run both focused commands, `npm run lint`, `npm run format:check`, `npm run typecheck`, `npm audit --omit=dev`, and `npm ls modbus-serial --depth=0`.
</verification>

<success_criteria>

- Only exact modbus-serial 8.0.25 is a direct runtime dependency.
- Adapter behavior is exact, isolated, mocked, and retry-neutral.
- Normal construction uses the real adapter internally.
- Public types, errors, diagnostics, and constructor remain adapter-free.

</success_criteria>

<output>
Create `.planning/phases/02-modbus-reads-detection-and-resilience/02-08-SUMMARY.md` when done.
</output>
