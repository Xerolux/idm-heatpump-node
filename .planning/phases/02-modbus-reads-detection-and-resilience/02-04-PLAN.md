---
phase: 02-modbus-reads-detection-and-resilience
plan: "04"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/client/diagnostics.ts
  - src/transport/errors.ts
  - src/transport/logging.ts
  - test/client/diagnostics.test.ts
  - test/client/errors.test.ts
autonomous: true
requirements:
  - TRN-01
  - DET-02
  - ERR-01R
must_haves:
  truths:
    - "Only structured numeric Code 2 creates IllegalAddressError; the public class adds no unmapped `code` member."
    - "quietPymodbusLogging accepts Python logging names and aliases CRITICAL/FATAL, ERROR, WARNING/WARN, INFO, DEBUG, and NOTSET case-insensitively plus numeric levels."
    - "ModbusErrorContext and IdmClientDiagnostics factories clone, freeze, and preserve caller sequence order exactly."
    - "Only IdmModbusClient.getDiagnostics later sorts snapshots built from internal sets; public factories never sort semantic arrays."
  artifacts:
    - path: "src/transport/errors.ts"
      provides: "Public IllegalAddressError and internal closed normalized failures"
      exports: ["IllegalAddressError"]
    - path: "src/transport/logging.ts"
      provides: "Python-compatible adapter-neutral logging helper"
      exports: ["quietPymodbusLogging"]
    - path: "src/client/diagnostics.ts"
      provides: "Exact immutable ModbusErrorContext and IdmClientDiagnostics factories"
      exports: ["ModbusErrorContext", "IdmClientDiagnostics"]
    - path: "test/client/errors.test.ts"
      provides: "Code-2, aliases, normalized errors, and redaction evidence"
    - path: "test/client/diagnostics.test.ts"
      provides: "Exact fields/defaults/order/ownership evidence"
  key_links:
    - from: "src/transport/errors.ts"
      to: "contracts/normalization.md"
      via: "closed normalized failure kinds and message projection"
      pattern: "illegal_address|invalid_response|no_response"
    - from: "src/client/diagnostics.ts"
      to: "test/fixtures/public-classes.json"
      via: "exact dataclass constructor/member/default closure"
      pattern: "IdmClientDiagnostics|ModbusErrorContext"
---

<objective>
Implement the exact public Phase-2 errors, logging helper, and immutable data values required by D-04, D-13, and D-16.

Purpose: Give lifecycle/read code stable public outcomes without inventing members, sorting caller sequences, or exposing adapter errors.
Output: IllegalAddressError, closed internal failures, logging aliases, ModbusErrorContext, IdmClientDiagnostics, and focused tests.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@contracts/api-mapping.json
@contracts/normalization.md
@src/errors.ts
@src/types.ts
@test/fixtures/public-classes.json
@.planning/phases/02-modbus-reads-detection-and-resilience/02-CONTEXT.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-RESEARCH.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-01-SUMMARY.md

<interfaces>
Pinned `ModbusErrorContext` fields are operation, address, count, register type, error type, message, and one-based attempt. Pinned `IdmClientDiagnostics` fields are navigator type, Modbus-connected, nullable firmware, nullable last error, permanently-failed names, connection-suspect, and batch-unsafe names. Unsupported names are intentionally absent.

Python dataclass construction preserves tuple/list argument order. Therefore both public factories preserve input array order. Later `IdmModbusClient.getDiagnostics` sorts its own internal sets before calling the factory because pinned Python sorts those sets at that call site.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-04-01: Implement structured failures, exact IllegalAddressError, and logging aliases</name>
  <files>src/transport/errors.ts, src/transport/logging.ts, test/client/errors.test.ts</files>
  <read_first>
    - src/errors.ts
    - contracts/normalization.md
    - contracts/api-mapping.json
    - test/fixtures/public-classes.json
    - pinned `ad121ebf34a5f5e37204371c026927d77efcd15c:idm_heatpump/client.py` `quiet_pymodbus_logging`, `IllegalAddressError`, `_retry_command`, and `_record_error_context`
    - Python `logging.getLevelName` behavior for CRITICAL/FATAL, ERROR, WARNING/WARN, INFO, DEBUG, and NOTSET
  </read_first>
  <behavior>
    - IllegalAddressError is a named Error with `isIllegalAddress === true`, normal Error fields, and no additional public `code`, cause, adapter, response, or raw field.
    - Numeric Modbus code 2 maps to illegal address; message-only and localized variants do not.
    - Internal failures use only the seven Plan-01 kinds and bounded endpoint-redacted text.
    - Logging accepts CRITICAL/FATAL=50, ERROR=40, WARNING/WARN=30, INFO=20, DEBUG=10, NOTSET=0, any case variant, and numeric pass-through.
    - Unknown strings throw and a missing neutral logging hook is a no-op.
  </behavior>
  <action>
Write RED tests for every failure kind, numeric Code 2, forged text, exact public property closure, endpoint redaction, bounded diagnostics, all canonical logging names, FATAL/WARN aliases, lowercase/mixed-case variants, numeric pass-through, no hook, and unknown strings. Implement the named public error following `src/errors.ts` without a `code` field. Implement internal normalized failure/redaction helpers from Plan 01 and an internal logging-hook registration seam for the real adapter.
  </action>
  <verify>
    <automated>npm test -- test/client/errors.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - IllegalAddressError has only mapped semantic and normal Error members.
    - Numeric Code 2 is the sole illegal-address classifier.
    - WARN/FATAL and every lowercase/mixed-case alias map exactly to Python numeric levels.
    - Raw adapter values and configured endpoint text cannot be reached publicly.
  </acceptance_criteria>
  <done>Retry and adapter layers have an exact safe error language and a Python-compatible neutral logging helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-04-02: Implement order-preserving immutable context and diagnostics factories</name>
  <files>src/client/diagnostics.ts, test/client/diagnostics.test.ts</files>
  <read_first>
    - src/types.ts
    - test/fixtures/public-classes.json
    - test/semantic/constants-and-types.test.ts
    - contracts/normalization.md
    - pinned `ad121ebf34a5f5e37204371c026927d77efcd15c:idm_heatpump/client.py` dataclasses and `get_diagnostics`
  </read_first>
  <behavior>
    - ModbusErrorContext.create requires exactly seven camelCase fields and returns a frozen owned object.
    - IdmClientDiagnostics.create requires navigatorType and modbusConnected, applies exact nullable/boolean/array defaults, clones arrays in caller order, and freezes arrays/object.
    - Neither factory sorts permanentlyFailedRegisters or batchUnsafeRegisters.
    - Exact runtime keys exclude unsupported names, raw errors, endpoint, slave ID, request objects, and write payloads.
    - Caller mutation and hostile casts cannot change snapshots.
  </behavior>
  <action>
Write RED fixture-derived tests for exact keys, defaults, naming, duplicate names, caller-order preservation (including deliberately non-sorted arrays), nulls, and hostile mutation. Implement same-name interfaces plus frozen `.create()` namespaces following `IdmModelInfo`, but clone/freeze arrays without sorting. Keep sanitization/error-kind normalization at the internal construction boundary; the public factory must not reinterpret semantic inputs.
  </action>
  <verify>
    <automated>npm test -- test/client/diagnostics.test.ts -t "factory|order|default|immutable|keys" &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Runtime keys/defaults exactly match both pinned dataclasses after approved naming and tuple normalization.
    - Deliberately non-sorted input arrays remain in the same order.
    - Returned objects and collections are newly owned and mutation-safe.
    - Unsupported/adapter/endpoint/write fields are absent.
  </acceptance_criteria>
  <done>The client can publish exact immutable values while preserving the semantic sequence contract.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                    |
| ------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Adapter error → normalized failure    | Untrusted errors must not select privileged retry semantics or leak endpoint details.          |
| Internal/client input → public values | Mutable arrays and contexts must become owned immutable snapshots without semantic reordering. |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                          |
| --------- | ---------------------- | ------------------------ | ----------- | -------------------------------------------------------- |
| T-02-03   | Spoofing               | Code-2 classification    | mitigate    | Numeric structured evidence only; forged-message tests.  |
| T-02-04   | Information disclosure | failures and diagnostics | mitigate    | Exact redaction, bounded text, no cause/raw object.      |
| T-02-02   | Tampering              | public snapshots         | mitigate    | Clone/freeze while preserving order; hostile-cast tests. |
| T-02-SC   | Tampering              | package supply chain     | accept      | No dependency installation occurs in this plan.          |

No ASVS L1 high-severity item may remain open at plan completion.
</threat_model>

<artifacts_produced>

## Artifacts This Phase Produces

| Artifact                                | Purpose                                                |
| --------------------------------------- | ------------------------------------------------------ |
| `IllegalAddressError`                   | Exact public Code-2 marker without additive members    |
| normalized failure domain               | Stable internal retry/fallback input                   |
| `quietPymodbusLogging`                  | Python-compatible aliases through neutral hook         |
| immutable context/diagnostics factories | Exact dataclass fields, defaults, order, and ownership |

</artifacts_produced>

<verification>
Run both focused commands, `npm run lint`, `npm run format:check`, and `npm run typecheck`. Inspect emitted/public declarations to confirm no `code`, raw cause, adapter type, endpoint, or sorted-factory promise exists.
</verification>

<success_criteria>

- Numeric Code 2 is structured, immediate, and unforgeable by text.
- WARN/FATAL aliases and canonical logging levels match Python.
- Public factories preserve caller order while cloning and freezing.
- All three mapped public values/functions remain unpromoted until Plan 02-09 evidence.

</success_criteria>

<output>
Create `.planning/phases/02-modbus-reads-detection-and-resilience/02-04-SUMMARY.md` when done.
</output>
