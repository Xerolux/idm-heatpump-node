---
phase: 02-modbus-reads-detection-and-resilience
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - scripts/check-parity.mjs
  - scripts/generate-python-contract.py
  - test/fixtures/transport-behavior.json
  - test/parity/generator.test.ts
  - test/parity/transport-contract.test.ts
autonomous: true
requirements:
  - TRN-01
  - TRN-03R
  - DET-01
  - DET-02
  - ERR-01R
must_haves:
  truths:
    - "The pinned Python generator produces `transport-behavior.json` as the seventh fixture and ninth generated artifact overall."
    - "Every runtime case has exact baseline provenance, all eight CTR-01 fields, closed normalized errors, deterministic bytes, and synthetic data only."
    - "Parity generation/check stages seven fixtures plus two generated documents transactionally and check mode is byte- and mtime-non-mutating."
    - "The TypeScript inventory parses every generated case before later behavior plans execute it."
  artifacts:
    - path: "test/fixtures/transport-behavior.json"
      provides: "Pinned Python lifecycle/read/detection/resilience scenarios"
    - path: "scripts/generate-python-contract.py"
      provides: "Exact seven-fixture Python generator"
    - path: "scripts/check-parity.mjs"
      provides: "Nine-artifact isolated transactional orchestrator"
    - path: "test/parity/generator.test.ts"
      provides: "Count, provenance, determinism, drift, and rollback evidence"
  key_links:
    - from: "scripts/generate-python-contract.py"
      to: "contracts/normalization.md"
      via: "closed errorType and message projection"
      pattern: "transport_error_type_to_closed_kind|diagnostic_message_redaction"
    - from: "scripts/check-parity.mjs"
      to: "test/fixtures/transport-behavior.json"
      via: "fixed seven-fixture/nine-artifact allowlist"
      pattern: "transport-behavior"
---

<objective>
Generate and orchestrate exact pinned Python runtime evidence required by D-05 through D-16 and G-03.

Purpose: Make Python request traces, results, controlled time, and final state the source of truth before TypeScript client logic is implemented.
Output: Seventh fixture, nine-artifact transactional pipeline, closed inventory tests, and parser-backed generated evidence.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@UPSTREAM-PARITY.json
@contracts/normalization.md
@scripts/generate-python-contract.py
@scripts/check-parity.mjs
@test/parity/generator.test.ts
@src/contracts/transport-scenario.ts
@test/parity/transport-contract.test.ts
@/mnt/c/Users/basti/Documents/GitHub/idm-heatpump-api/AGENTS.md
@/mnt/c/Users/basti/Documents/GitHub/idm-heatpump-api/docs/Register-Map-Invariants.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-CONTEXT.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-RESEARCH.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-02-SUMMARY.md

<interfaces>
The current Python generator emits six fixtures. Adding `test/fixtures/transport-behavior.json` makes seven Python fixtures. `scripts/check-parity.mjs` also stages generated `docs/API-PARITY.md` and `docs/BASELINE.md`, making nine generated artifacts overall.

The generator must project Python runtime errors through the exact Plan-01 error-kind table and endpoint-redaction algorithm before JSON serialization. TypeScript parsing and later execution compare the same normalized fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-03-01: Generate the seventh pinned Python behavior fixture</name>
  <files>scripts/generate-python-contract.py, test/fixtures/transport-behavior.json, test/parity/generator.test.ts, test/parity/transport-contract.test.ts</files>
  <read_first>
    - scripts/generate-python-contract.py
    - test/parity/generator.test.ts
    - src/contracts/transport-scenario.ts
    - contracts/normalization.md
    - pinned `ad121ebf34a5f5e37204371c026927d77efcd15c:idm_heatpump/client.py`
    - pinned `ad121ebf34a5f5e37204371c026927d77efcd15c:tests/fake_modbus.py`
    - pinned `ad121ebf34a5f5e37204371c026927d77efcd15c:tests/test_client.py`
    - pinned `ad121ebf34a5f5e37204371c026927d77efcd15c:tests/test_fake_modbus_transport.py`
  </read_first>
  <behavior>
    - `OUTPUT_PATHS` contains exactly seven fixture paths and labels transport behavior as the seventh fixture.
    - Cases cover constructor/lifecycle, serialization, FC03/FC04, official overlaps, retries/delays, fallback, unsupported/permanent/quarantine, all detection variants, diagnostics, and reset/query state.
    - Every case contains all eight CTR-01 fields and one exact normalized error/message projection when relevant.
    - Generation uses a deterministic Python fake plus controlled sleep/monotonic time and never opens TCP or reads ambient sibling HEAD.
    - Generated bytes are canonical with one trailing newline and contain no Navigator-1, private endpoint, PIN, identifier, or raw capture.
  </behavior>
  <action>
Write RED tests for the exact seven-fixture count, required unique case categories, eight-field envelopes, deterministic bytes, source-derived requests/state, symmetric errorType/message normalization, synthetic endpoint, and forbidden-data scans. Extend the verified generator only after its existing exact identity gate. Use pinned client methods and a deterministic fake/monkeypatch to record lifecycle requests, read requests, attempts, delays, results, and state; do not hand-copy TypeScript expectations or import target implementation values.
  </action>
  <verify>
    <automated>npm test -- test/parity/generator.test.ts test/parity/transport-contract.test.ts -t "transport|seven|fixture|normalization|deterministic"</automated>
  </verify>
  <acceptance_criteria>
    - The exact pinned checkout produces seven canonical fixtures.
    - Transport behavior includes the complete minimum scenario matrix and parses through the closed TypeScript parser.
    - Python errorType/message fields already use the reviewed language-neutral projection.
    - No network/device/sensitive input participates in generation.
  </acceptance_criteria>
  <done>The exact Python client now emits reproducible runtime evidence for all Phase-2 observable behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-03-02: Orchestrate seven fixtures and two documents transactionally</name>
  <files>scripts/check-parity.mjs, test/parity/generator.test.ts, test/parity/transport-contract.test.ts</files>
  <read_first>
    - scripts/check-parity.mjs
    - test/parity/generator.test.ts
    - test/fixtures/transport-behavior.json
    - .planning/phases/01-reproducible-semantic-contract/01-07-SUMMARY.md
    - .planning/phases/01-reproducible-semantic-contract/01-09-SUMMARY.md
  </read_first>
  <behavior>
    - `GENERATED_PATHS` contains exactly seven fixture paths plus `docs/API-PARITY.md` and `docs/BASELINE.md`.
    - Check mode compares all nine artifacts without changing bytes or mtimes.
    - Generate mode stages and replaces all nine artifacts as one transaction and restores all prior artifacts after injected replacement failure.
    - Symlinks, missing paths, extra generated paths, oversized child output, and drift fail with stable codes.
    - The parser inventory requires all generated scenario names unique and all scripts structurally consumable.
  </behavior>
  <action>
Write RED count, drift, mtime, symlink, extra-path, and rollback tests using the exact phrases “seven fixtures”, “two generated documents”, and “nine generated artifacts”. Extend the isolated `-I` shadow-root pipeline and fixed allowlists. Keep check mode structurally non-mutating and preserve verify-before-import. Add parser inventory assertions only; behavior execution remains in Plans 02-06 and 02-07.
  </action>
  <verify>
    <automated>npm test -- test/parity/generator.test.ts test/parity/transport-contract.test.ts -t "nine|allowlist|check mode|rollback|inventory" &amp;&amp; npm run parity:check &amp;&amp; git diff --exit-code -- test/fixtures</automated>
  </verify>
  <acceptance_criteria>
    - All count language and assertions consistently report seven fixtures and nine total generated artifacts.
    - Check mode is byte- and mtime-non-mutating for every generated path.
    - Replacement failure restores all nine prior artifacts.
    - The fixed pipeline has no ambient-checkout or unbounded-output shortcut.
  </acceptance_criteria>
  <done>Phase-2 runtime evidence participates in the same exact isolated and transactional parity lifecycle as every earlier generated artifact.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                 |
| ------------------------------------------- | --------------------------------------------------------------------------- |
| Pinned Python execution → generated fixture | Reference execution must be exact, isolated, synthetic, and bounded.        |
| Staged artifacts → repository               | Generate/check must not partially replace or mutate the fixed evidence set. |

## STRIDE Threat Register

| Threat ID | Category                      | Component                      | Disposition | Mitigation Plan                                                                |
| --------- | ----------------------------- | ------------------------------ | ----------- | ------------------------------------------------------------------------------ |
| T-02-04   | Information disclosure        | generated scenarios            | mitigate    | Synthetic endpoint/data and forbidden-content scans.                           |
| T-02-06   | Denial of service / Tampering | Python process and artifacts   | mitigate    | Verify-before-import, `-I`, bounded output/time, fixed paths, atomic rollback. |
| T-02-03   | Spoofing                      | generated error classification | mitigate    | Closed source-type table and numeric Code-2 evidence before serialization.     |
| T-02-SC   | Tampering                     | package supply chain           | accept      | No npm dependency is installed in this plan.                                   |

No ASVS L1 high-severity item may remain open at plan completion.
</threat_model>

<artifacts_produced>

## Artifacts This Phase Produces

| Artifact                   | Purpose                                               |
| -------------------------- | ----------------------------------------------------- |
| `transport-behavior.json`  | Seventh pinned Python fixture                         |
| seven-fixture generator    | Exact runtime evidence generation                     |
| nine-artifact orchestrator | Transactional fixtures plus API/baseline documents    |
| generator/inventory tests  | Determinism, drift, rollback, and normalization proof |

</artifacts_produced>

<verification>
Run both focused commands, `npm run lint`, `npm run format:check`, `npm run typecheck`, and non-mutating `npm run parity:check`. Confirm the six existing fixture bytes change only when their pinned facts actually change.
</verification>

<success_criteria>

- The count is exactly seven Python fixtures and nine generated artifacts overall.
- Runtime behavior is source-generated, normalized symmetrically, deterministic, and synthetic.
- Check mode is non-mutating and generate mode is fully transactional.
- Later client plans can execute a closed complete scenario inventory.

</success_criteria>

<output>
Create `.planning/phases/02-modbus-reads-detection-and-resilience/02-03-SUMMARY.md` when done.
</output>
