---
phase: 02-modbus-reads-detection-and-resilience
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/normalization.md
  - contracts/typescript-extensions.json
  - docs/API-PARITY.md
  - docs/PARITY-CONTRACT.md
  - scripts/generate-api-parity.mjs
  - test/parity/api-parity.test.ts
autonomous: true
requirements:
  - TRN-01
  - TRN-03R
  - ERR-01R
must_haves:
  truths:
    - "The exact Python public inventory remains 89/89 while `ModbusTransport` has a separate closed additive authority."
    - "A private-development `partial` IdmModbusClient is release-blocking and machine-partitions every pinned member into implemented or omitted sets."
    - "The only public constructor normalization is required host plus mapped camelCase options; pymodbus retries are internalized at zero and transport/time injection is not public."
    - "Runtime errorType and message projections are closed, symmetric between Python generation and TypeScript execution, and reject undocumented equivalence."
  artifacts:
    - path: "contracts/typescript-extensions.json"
      provides: "Closed additive TypeScript-only API authority"
      contains: "ModbusTransport"
    - path: "contracts/normalization.md"
      provides: "Reviewed constructor, errorType, and diagnostic-redaction rules"
      contains: "transport_error_type_to_closed_kind"
    - path: "scripts/generate-api-parity.mjs"
      provides: "Independent extension and partial-class validation"
    - path: "test/parity/api-parity.test.ts"
      provides: "Mutation and release-block evidence for governance rules"
  key_links:
    - from: "contracts/typescript-extensions.json"
      to: "scripts/generate-api-parity.mjs"
      via: "separate schema validation and generated projection"
      pattern: "no_python_counterpart|typescript-extensions"
    - from: "contracts/normalization.md"
      to: "test/parity/api-parity.test.ts"
      via: "closed normalization allowlist mutation tests"
      pattern: "transport_error_type_to_closed_kind|diagnostic_message_redaction"
---

<objective>
Establish the Phase-2 public-governance and runtime-normalization authority required by D-01, D-04, D-13, D-16, and G-01 through G-04.

Purpose: Prevent additive Node types, a cross-phase client class, public dependency injection, or runtime-specific error text from weakening exact Python parity.
Output: Separate TypeScript-extension governance, exact partial-class lifecycle rules, reviewed constructor normalization, and symmetric closed error projections.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@UPSTREAM-PARITY.json
@contracts/api-mapping.json
@contracts/normalization.md
@docs/PARITY-CONTRACT.md
@scripts/generate-api-parity.mjs
@test/fixtures/public-api.json
@test/fixtures/public-classes.json
@test/parity/api-parity.test.ts
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-CONTEXT.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-RESEARCH.md

<interfaces>
The Python mapping remains exactly 89 ordered rows. `ModbusTransport` has no Python counterpart and therefore belongs only to a separate additive authority.

The public `IdmModbusClient` constructor may normalize Python positional/keyword configuration to `new IdmModbusClient(host, options?)`, where options contain only `port`, `slaveId`, `timeout`, `maxRetries`, and `maxGroupSize`. Python `pymodbus_retries` is fixed internally to zero. Transport factory, monotonic clock, and sleep dependencies are accessible only through an internal source/test seam that is absent from root declarations and API mapping evidence.

The closed runtime error kind set is `timeout`, `disconnected`, `socket`, `no_response`, `modbus`, `illegal_address`, and `invalid_response`. Python and TypeScript must apply the same source-to-kind table before comparison. Diagnostic messages replace the configured `host:port`, bracketed host/port, and bare configured host—longest token first—with the literal `<endpoint>`, preserve all other text exactly, and reject overlong output rather than using a permissive catch-all.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-01-01: Govern additive exports and exact partial classes</name>
  <files>contracts/typescript-extensions.json, scripts/generate-api-parity.mjs, test/parity/api-parity.test.ts, docs/API-PARITY.md, docs/PARITY-CONTRACT.md</files>
  <read_first>
    - contracts/api-mapping.json
    - test/fixtures/public-api.json
    - test/fixtures/public-classes.json
    - scripts/generate-api-parity.mjs
    - test/parity/api-parity.test.ts
    - docs/API-PARITY.md
    - docs/PARITY-CONTRACT.md
    - .planning/phases/01-reproducible-semantic-contract/01-03-SUMMARY.md
    - .planning/phases/01-reproducible-semantic-contract/01-09-SUMMARY.md
  </read_first>
  <behavior>
    - The Python mapping remains exactly 89 unique rows in pinned order.
    - The extension file is closed to explicit additive symbols with owner, status, export path, kind, rationale, evidence, and `no_python_counterpart: true`.
    - A partial class declares exact camelCase implemented and omitted members whose union equals the pinned fixture and whose intersection is empty.
    - Development generation documents partial/extensions separately; release validation rejects every partial mapping and every non-complete extension.
    - `ModbusTransport` begins planned and no synthetic Python mapping row is created.
  </behavior>
  <action>
Write RED mutation tests for unknown/missing extension fields, duplicate or fabricated symbols, invalid evidence paths, partial gaps/overlap/duplicates, changed Python inventory, and release acceptance of partial/planned entries. Create schema version 1 of `contracts/typescript-extensions.json` with one planned type-only `ModbusTransport` row. Extend the generator with independent extension validation and exact partial-member partition checks. Document G-01/G-02 and clause-aware G-04 policy durably, regenerate `docs/API-PARITY.md`, and keep release mode red. Never use throwing Phase-3 stubs as member evidence.
  </action>
  <verify>
    <automated>npm test -- test/parity/api-parity.test.ts -t "extension|partial|89|release" &amp;&amp; npm run parity:api</automated>
  </verify>
  <acceptance_criteria>
    - The Python mapping count and rows are unchanged.
    - Malformed additive and partial authorities fail deterministically.
    - `ModbusTransport` is separately planned and the generated document distinguishes it from Python coverage.
    - Release mode cannot pass with any partial client or incomplete extension.
  </acceptance_criteria>
  <done>Additive TypeScript surface and cross-phase Python classes have explicit machine governance without a parity overclaim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-01-02: Define closed constructor and runtime error normalizations</name>
  <files>contracts/normalization.md, scripts/generate-api-parity.mjs, test/parity/api-parity.test.ts, docs/PARITY-CONTRACT.md</files>
  <read_first>
    - contracts/normalization.md
    - contracts/api-mapping.json
    - test/fixtures/public-classes.json
    - pinned `ad121ebf34a5f5e37204371c026927d77efcd15c:idm_heatpump/client.py` constructor, `_record_error_context`, and retry exceptions
    - pinned `ad121ebf34a5f5e37204371c026927d77efcd15c:tests/test_client.py`
    - pinned `ad121ebf34a5f5e37204371c026927d77efcd15c:tests/test_fake_modbus_transport.py`
  </read_first>
  <behavior>
    - The options-object normalization admits only port, slaveId, timeout, maxRetries, and maxGroupSize; injection fields and pymodbusRetries are rejected from public evidence.
    - `transport_error_type_to_closed_kind` maps numeric Code 2/IllegalAddressError to illegal_address, timeout exceptions to timeout, ConnectionException to disconnected, socket/OS errors to socket, ModbusIOException/no-response evidence to no_response, other ModbusException to modbus, and exact malformed-response evidence to invalid_response.
    - `diagnostic_message_redaction` performs the same longest-first endpoint replacement in Python generation and TypeScript execution, preserves remaining text/order, and enforces one documented bound.
    - No class name, message substring, case folding, or undocumented fallback can establish error equivalence.
  </behavior>
  <action>
Add the constructor-options, internalized-adapter-retry, closed error-kind, and diagnostic-redaction normalization codes to the authoritative allowlist and documentation. Add mutation tests proving unknown kinds, message-only Code 2, extra public injection options, asymmetric redaction, alternate placeholders, and overlong diagnostics fail. Specify that the later generator and runner must consume these exact rules symmetrically; do not sort request traces or constructor/member sequences.
  </action>
  <verify>
    <automated>npm test -- test/parity/api-parity.test.ts -t "constructor|normalization|error|redaction" &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Public constructor authority contains no transport, clock, sleep, or adapter retry option.
    - The seven error kinds have one closed source mapping.
    - Endpoint redaction is deterministic, symmetric, and bounded.
    - Undocumented exception/message equivalence remains forbidden.
  </acceptance_criteria>
  <done>Later fixtures and client code share one exact language-neutral runtime error projection and one exact public constructor boundary.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                               |
| ---------------------------------------- | ----------------------------------------------------------------------------------------- |
| Python inventory → additive Node surface | Node-only types and partial classes must not falsify 89/89 Python coverage.               |
| Runtime errors → parity values           | Adapter/Python exception classes and endpoint-bearing messages require closed projection. |

## STRIDE Threat Register

| Threat ID | Category               | Component                           | Disposition | Mitigation Plan                                                         |
| --------- | ---------------------- | ----------------------------------- | ----------- | ----------------------------------------------------------------------- |
| T-02-03   | Spoofing               | illegal-address normalization       | mitigate    | Numeric Code 2 or structured marker only; message-only mutation tests.  |
| T-02-04   | Information disclosure | diagnostic projection               | mitigate    | Exact endpoint replacement, bounded output, no raw cause/payload.       |
| T-02-06   | Tampering              | API mapping and extension authority | mitigate    | Independent closed schemas, exact member partition, hard release block. |
| T-02-SC   | Tampering              | package supply chain                | accept      | No dependency installation occurs in this plan.                         |

No ASVS L1 high-severity item may remain open at plan completion.
</threat_model>

<artifacts_produced>

## Artifacts This Phase Produces

| Artifact                               | Purpose                                                      |
| -------------------------------------- | ------------------------------------------------------------ |
| `contracts/typescript-extensions.json` | Separate additive TypeScript public authority                |
| partial-class lifecycle validation     | Exact private-development member partition and release block |
| runtime error normalization rules      | Symmetric Python/TypeScript errorType and message projection |
| constructor normalization rules        | Public mapped options only; internal injection stays private |

</artifacts_produced>

<verification>
Run both focused commands, `npm run lint`, `npm run format:check`, `npm run typecheck`, and `npm run parity:api`. Confirm the Python mapping is still exactly 89 rows and release validation remains red.
</verification>

<success_criteria>

- G-01 through G-04 are machine-readable and release-safe.
- Public injection and adapter retry options cannot leak into mapped declarations.
- Runtime error kinds and endpoint redaction are closed and symmetric.
- No generated document claims the partial client or planned transport type complete.

</success_criteria>

<output>
Create `.planning/phases/02-modbus-reads-detection-and-resilience/02-01-SUMMARY.md` when done.
</output>
