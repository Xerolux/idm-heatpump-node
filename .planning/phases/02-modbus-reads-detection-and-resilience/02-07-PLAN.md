---
phase: 02-modbus-reads-detection-and-resilience
plan: "07"
type: execute
wave: 5
depends_on:
  - 02-06
files_modified:
  - src/client/detection.ts
  - src/client/idm-modbus-client.ts
  - test/client/detection.test.ts
  - test/client/diagnostics.test.ts
  - test/parity/transport-contract.test.ts
autonomous: true
requirements:
  - TRN-02
  - DET-01
  - DET-02
  - ERR-01R
must_haves:
  truths:
    - "Detection probes exact addresses/counts in pinned order with one attempt and two-second timeout without changing register counters."
    - "Circuit, zone, solar, ISC, PV, cascade, Navigator-10, firmware, sentinel, and two-missing-stop semantics match Python."
    - "Model priority and modelName fallback match Python, and immutable IdmModelInfo drives the exact Phase-1 register map."
    - "getDiagnostics sorts only snapshots built from internal sets before calling the order-preserving factory."
    - "Generated detection/diagnostic scenarios match result, probes, timeouts, map projection, normalized errors, and final state."
  artifacts:
    - path: "src/client/detection.ts"
      provides: "Explicit ordered detection/classification service"
    - path: "src/client/idm-modbus-client.ts"
      provides: "detectModel, model cache, queries, resets, and diagnostics integration"
    - path: "test/client/detection.test.ts"
      provides: "All model/capability/sentinel/firmware/map evidence"
    - path: "test/client/diagnostics.test.ts"
      provides: "Client-integrated sorting, ownership, and reset evidence"
    - path: "test/parity/transport-contract.test.ts"
      provides: "Executable generated detection/diagnostic scenarios"
  key_links:
    - from: "src/client/detection.ts"
      to: "src/client/idm-modbus-client.ts"
      via: "locked probe callback and immutable result"
      pattern: "detectModel|probe"
    - from: "src/client/idm-modbus-client.ts"
      to: "src/registers/registry.ts"
      via: "detected model-aware cached map"
      pattern: "buildRegisterMap"
    - from: "src/client/idm-modbus-client.ts"
      to: "src/client/diagnostics.ts"
      via: "sorted internal set snapshots passed to preserving factory"
      pattern: "IdmClientDiagnostics.create"
---

<objective>
Implement the complete ordered detection and public state/diagnostics slice required by D-09 through D-12 and D-16.

Purpose: Turn exact probe responses into the same model, features, map, and safe observable state as the pinned Python client.
Output: Detection service, completed Phase-2 state methods, focused tests, and executable detection scenarios.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@/mnt/c/Users/basti/Documents/GitHub/idm-heatpump-api/docs/Register-Map-Invariants.md
@src/constants.ts
@src/types.ts
@src/codec.ts
@src/registers/registry.ts
@src/client/diagnostics.ts
@src/client/idm-modbus-client.ts
@test/fixtures/transport-behavior.json
@.planning/phases/02-modbus-reads-detection-and-resilience/02-CONTEXT.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-RESEARCH.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-PATTERNS.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-06-SUMMARY.md

<interfaces>
Detection order is circuits `1350 + 2*n / 2`, zones `2000 + 65*n / 1`, solar `1850/2`, ISC `1870/2`, PV `74/2`, cascade `1147/1`, Navigator 10 `4108/2`, then optional firmware `4120/2`.

Circuit `-1.0` is unavailable. Two consecutive missing circuit or zone slots stop each scan. An exact circuit pair that is undecodable/out-of-range is still active. Exact solar/ISC pairs prove presence. Cascade low byte 255 is absent and 0 is present.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-07-01: Implement exact ordered model and capability detection</name>
  <files>src/client/detection.ts, src/client/idm-modbus-client.ts, test/client/detection.test.ts</files>
  <read_first>
    - src/constants.ts
    - src/types.ts
    - src/codec.ts
    - src/registers/registry.ts
    - src/client/idm-modbus-client.ts
    - /mnt/c/Users/basti/Documents/GitHub/idm-heatpump-api/docs/Register-Map-Invariants.md
    - pinned `client.py` `probe_register`, `_probe_model_register`, `_probe_float_value`, and `detect_model`
    - pinned detection cases in `tests/test_client.py`
  </read_first>
  <behavior>
    - Probe trace follows the exact table and stops scans after two consecutive missing/unavailable slots.
    - Circuit -1.0 is missing; exact invalid/NaN/out-of-range pairs are active; solar/ISC/PV exact pairs are present.
    - Cascade 255 is absent and 0 is present; 4108 chooses Navigator 10; zones choose Pro; circuits choose Navigator 2.0; otherwise Unknown.
    - readFirmware false emits no 4120 request; valid finite firmware uses Python-equivalent two-decimal rounding; invalid/missing is null.
    - Every probe uses one attempt and timeoutMs 2000 and leaves unsupported/permanent/batch-unsafe state unchanged.
    - Model/features are immutable and rebuild/cache the exact Phase-1 map without Navigator-1 facts.
  </behavior>
  <action>
Write RED table tests for all four models, every capability, stop rules, -1 sentinel, undecodable/out-of-range pairs, cascade 0/255, firmware on/off/invalid, exact order/count/timeout, unchanged failure state, and map inclusion/exclusion. Implement explicit ordered detection rather than inferring all probes from `getDetectionRegisters`, because 4108 and 4120 are direct probes. Use existing codec, `IdmModelInfo.create`, and `buildRegisterMap`; invalidate the cache only after a complete result.
  </action>
  <verify>
    <automated>npm test -- test/client/detection.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Every model/capability/sentinel/stop/firmware case matches result and exact trace.
    - Temporary timeout appears only on probe requests.
    - Detection leaves register failure/quarantine state unchanged.
    - Model-aware maps contain no Navigator-1 address or constant.
  </acceptance_criteria>
  <done>Identical probes produce identical immutable model, feature, firmware, and register-map outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-07-02: Integrate diagnostics, exact reset/query state, and detection scenarios</name>
  <files>src/client/idm-modbus-client.ts, test/client/diagnostics.test.ts, test/client/detection.test.ts, test/parity/transport-contract.test.ts</files>
  <read_first>
    - src/client/idm-modbus-client.ts
    - src/client/diagnostics.ts
    - src/client/detection.ts
    - src/contracts/canonical-order.ts
    - test/client/diagnostics.test.ts
    - test/parity/transport-contract.test.ts
    - test/fixtures/transport-behavior.json
    - pinned `client.py` diagnostics, model properties, state queries, and reset methods
  </read_first>
  <behavior>
    - getDiagnostics returns modelName fallback, connected state, firmware string/null, latest redacted message/null, sorted permanent names, suspect flag, and sorted batch-unsafe names.
    - Sorting happens while snapshotting internal sets; IdmClientDiagnostics.create receives and preserves that sorted order.
    - getLastErrorContext returns null or immutable context; clearLastErrorContext clears only it.
    - unsupported and batch-unsafe queries are sorted/immutable; no public permanent getter is invented.
    - resetFailedRegisters clears unsupported/permanent/transient only.
    - Generated detection/diagnostic cases compare model info, modelName, map keys/schema summary, requests/timeouts, normalized context, and final state.
  </behavior>
  <action>
Write client-integrated RED tests for exact diagnostics fields, deliberate unsorted internal-set insertion followed by sorted output, Unknown fallback, firmware formatting, ownership, clear-last-error, reset scope, and disconnected/reconnected snapshots. Complete state methods from one owner. Extend the closed parity runner with detectModel and diagnostics/state operations, requiring all scripted responses consumed and exact map projection equal Python.
  </action>
  <verify>
    <automated>npm test -- test/client/detection.test.ts test/client/diagnostics.test.ts test/parity/transport-contract.test.ts -t "detect|diagnostic|model|reset|state|sort" &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Diagnostics and query/reset methods match pinned fields, ordering, and scope.
    - Factory input ordering remains preserving while client snapshots are Python-sorted.
    - Every generated detection/diagnostic scenario passes every observable dimension.
    - No Phase-3 write/cyclic/throttle member exists.
  </acceptance_criteria>
  <done>The read client exposes complete Python-equivalent detected state and diagnostics with exact sorting responsibility.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                     |
| ------------------------------ | --------------------------------------------------------------- |
| Probe responses → model/map    | Malformed/sentinel values can activate wrong capabilities.      |
| Detection sequence → transport | Order/timeout/stop bugs can create excessive or stale requests. |
| Internal state → diagnostics   | Mutable or sensitive values require exact owned projection.     |

## STRIDE Threat Register

| Threat ID | Category               | Component             | Disposition | Mitigation Plan                                                            |
| --------- | ---------------------- | --------------------- | ----------- | -------------------------------------------------------------------------- |
| T-02-01   | Denial of service      | circuit/zone probes   | mitigate    | Fixed limits, two-missing stop, one attempt, two-second timeout.           |
| T-02-02   | Tampering / race       | detection concurrency | mitigate    | Existing complete-operation FIFO and locked probes.                        |
| T-02-05   | Tampering              | sentinel/model map    | mitigate    | Exact counts, documented decoding, explicit probe table, Phase-1 builders. |
| T-02-04   | Information disclosure | diagnostics           | mitigate    | Synthetic fixtures, redacted context, immutable exact fields.              |
| T-02-SC   | Tampering              | package supply chain  | accept      | No dependency installation occurs in this plan.                            |

No ASVS L1 high-severity item may remain open at plan completion.
</threat_model>

<artifacts_produced>

## Artifacts This Phase Produces

| Artifact                            | Purpose                                                 |
| ----------------------------------- | ------------------------------------------------------- |
| ordered detection service           | Exact model/capability/sentinel/firmware classification |
| detectModel/model cache             | Serialized immutable detected state and map             |
| diagnostics/query/reset integration | Exact observable state and sorting                      |
| detection scenario execution        | Cross-repository probe/result/map/state proof           |

</artifacts_produced>

<verification>
Run both focused commands, `npm run lint`, `npm run format:check`, and `npm run typecheck`. Run Phase-1 register-schema parity and confirm no register definition/schema byte changed.
</verification>

<success_criteria>

- All ROADMAP detection outcomes are proven from exact ordered probes.
- Model/capability/map/sentinel/firmware behavior matches Python.
- Diagnostics and state queries/resets are exact, immutable, and correctly sorted.
- The generated scenario suite covers every Phase-2 domain behavior except real adapter and root promotion.

</success_criteria>

<output>
Create `.planning/phases/02-modbus-reads-detection-and-resilience/02-07-SUMMARY.md` when done.
</output>
