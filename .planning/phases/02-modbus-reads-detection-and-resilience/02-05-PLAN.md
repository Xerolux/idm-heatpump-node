---
phase: 02-modbus-reads-detection-and-resilience
plan: "05"
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-04
files_modified:
  - src/client/fifo-gate.ts
  - src/client/idm-modbus-client.ts
  - src/client/index.ts
  - src/client/internal-create.ts
  - test/client/lifecycle.test.ts
  - test/client/resilience.test.ts
autonomous: true
requirements:
  - TRN-01
  - TRN-02
  - ERR-01R
must_haves:
  truths:
    - "Every lifecycle, probe, and complete retry loop is FIFO-serialized through one non-reentrant client-owned gate."
    - "The public constructor exposes only required host and the five mapped camelCase options; transport/time dependencies exist only behind an internal direct-path test factory."
    - "Transport failures mark suspect, hard reconnect, and back off; generic Modbus/invalid-response failures retry the same connection; Code 2 never retries."
    - "Lifecycle state, exact attempts/delays, one-based normalized error context, and model fallback are deterministic under injected internal dependencies."
  artifacts:
    - path: "src/client/fifo-gate.ts"
      provides: "Private FIFO complete-operation primitive"
    - path: "src/client/idm-modbus-client.ts"
      provides: "Internal real client lifecycle/retry/probe state machine"
      exports: ["IdmModbusClient"]
    - path: "src/client/internal-create.ts"
      provides: "Direct-path-only test creation seam for transport factory, clock, and sleep"
    - path: "test/client/lifecycle.test.ts"
      provides: "Constructor, lifecycle, FIFO, and release-after-error evidence"
    - path: "test/client/resilience.test.ts"
      provides: "Retry, reconnect, delay, probe, and context evidence"
  key_links:
    - from: "src/client/internal-create.ts"
      to: "src/client/idm-modbus-client.ts"
      via: "unexported internal dependency token/factory"
      pattern: "InternalClientDependencies"
    - from: "src/client/idm-modbus-client.ts"
      to: "src/client/fifo-gate.ts"
      via: "one exclusive wrapper around complete public operations"
      pattern: "runExclusive|Locked"
    - from: "src/client/idm-modbus-client.ts"
      to: "src/transport/errors.ts"
      via: "closed retry-class discrimination and normalized context"
      pattern: "IllegalAddressError|NormalizedTransportFailure"
---

<objective>
Build the serialized lifecycle and retry state machine required by D-02 through D-04, D-09, D-13, and D-16.

Purpose: Establish exact connection recovery, operation ordering, and error state without exposing transport/time injection through the mapped constructor.
Output: FIFO gate, internal creation seam, real source client, lifecycle/retry/probe behavior, and focused tests.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@src/constants.ts
@src/types.ts
@src/transport/types.ts
@src/transport/errors.ts
@src/client/diagnostics.ts
@test/support/fake-modbus-transport.ts
@test/support/fake-clock.ts
@test/fixtures/public-classes.json
@.planning/phases/02-modbus-reads-detection-and-resilience/02-CONTEXT.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-RESEARCH.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-PATTERNS.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-02-SUMMARY.md
@.planning/phases/02-modbus-reads-detection-and-resilience/02-04-SUMMARY.md

<interfaces>
The emitted/public constructor overload is only `new IdmModbusClient(host, options?)`, with options `port`, `slaveId`, `timeout`, `maxRetries`, and `maxGroupSize`. The implementation may accept an unexported branded dependency token used only by `src/client/internal-create.ts`; that factory is imported directly by tests and is absent from `src/client/index.ts`, `src/index.ts`, declarations, and API authorities.

Public methods acquire the FIFO gate exactly once. Private `Locked` helpers assume ownership and may call one another without reacquiring it. The complete retry sequence—including ensure/connect, reconnect, delay, request, validation, and state mutation—remains inside the exclusive operation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-05-01: Implement exact constructor, internal injection seam, FIFO, and lifecycle</name>
  <files>src/client/fifo-gate.ts, src/client/idm-modbus-client.ts, src/client/index.ts, src/client/internal-create.ts, test/client/lifecycle.test.ts</files>
  <read_first>
    - src/constants.ts
    - src/types.ts
    - src/transport/types.ts
    - src/transport/errors.ts
    - src/client/diagnostics.ts
    - test/support/fake-modbus-transport.ts
    - test/support/fake-clock.ts
    - test/fixtures/public-classes.json
    - pinned `ad121ebf34a5f5e37204371c026927d77efcd15c:idm_heatpump/client.py` constructor and lifecycle methods
    - pinned lifecycle cases in `ad121ebf34a5f5e37204371c026927d77efcd15c:tests/test_client.py`
  </read_first>
  <behavior>
    - Empty host, invalid port/slave/retry/group values fail before any transport creation; timeout preserves Python's accepted numeric domain.
    - Defaults are port 502, slaveId 1, timeout 10, maxRetries 3, maxGroupSize 40; adapter retries are internally zero.
    - Root/client declarations and API evidence contain no transportFactory, clock, sleep, dependencies, token, or pymodbusRetries option.
    - Internal direct-path creation injects factory/clock/sleep and is unavailable from both package barrels.
    - Connect reuses healthy state; failed connect discards transport; disconnect closes/nulls only connection; forceReconnect closes/nulls, clears suspect, and reconnects.
    - modelInfo starts null and modelName falls back to Navigator 2.0.
    - 20+ mixed concurrent operations execute in arrival order, max-active stays one, and rejection does not poison the queue.
  </behavior>
  <action>
Write RED constructor, emitted-signature, barrel-closure, internal-factory, lifecycle, FIFO-order, and release-after-rejection tests. Implement a tail-safe promise FIFO. Implement the public overload and private branded dependency path so the only injectable creation API is direct-path internal. Keep one state owner and `Locked` helpers. Export only `IdmModbusClient` from the source client barrel; do not export the internal factory/token and do not add Phase-3 methods.
  </action>
  <verify>
    <automated>npm test -- test/client/lifecycle.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Exact defaults, validation, properties, and lifecycle state pass.
    - Public declarations expose only mapped constructor configuration.
    - Internal tests inject transport/time without root or barrel exposure.
    - FIFO ordering, one-active-operation, and recovery after rejection are proven.
  </acceptance_criteria>
  <done>The client has one exact serialized lifecycle and an internal-only deterministic dependency seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-05-02: Implement retry, reconnect, probe, and latest-error semantics</name>
  <files>src/client/idm-modbus-client.ts, test/client/lifecycle.test.ts, test/client/resilience.test.ts</files>
  <read_first>
    - src/client/idm-modbus-client.ts
    - src/transport/errors.ts
    - src/client/diagnostics.ts
    - test/support/fake-modbus-transport.ts
    - test/support/fake-clock.ts
    - contracts/normalization.md
    - pinned `ad121ebf34a5f5e37204371c026927d77efcd15c:idm_heatpump/client.py` `_retry_command`, `_read_registers`, `_try_reconnect`, `_record_error_context`, and probes
    - pinned retry/reconnect/probe tests in `tests/test_client.py` and `tests/test_fake_modbus_transport.py`
  </read_first>
  <behavior>
    - With maxRetries 3, attempts are 1,2,3 and inter-attempt delays are exactly 0.5,1.0.
    - timeout/disconnected/socket/no_response marks suspect, hard reconnects before the next attempt, and clears suspect only after successful I/O.
    - modbus and invalid_response retry on the same connection with the same delays.
    - IllegalAddressError records one normalized context and throws with one request, no reconnect, and no delay.
    - Exact response count and 16-bit words are validated before return.
    - probeRegister uses exact FC04, accepts temporary timeout/maxRetries, returns words or null for known client failures, and does not touch register failure/quarantine sets.
    - Latest context uses the closed errorType and redacted message, remains until explicit clear, and is immutable.
  </behavior>
  <action>
Write RED matrix tests for every normalized kind, reconnect success/failure, exact factory/close/connect/request counts, delays, attempts, Code 2, invalid count/word responses, clear-last-error, and one-attempt/two-second probes. Implement one locked retry runner and raw input-read helper. Never use message matching or an abandoning Promise.race. Keep the FIFO held through reconnect and delay. Catch only known normalized client failures in probe; configuration/programming errors still throw.
  </action>
  <verify>
    <automated>npm test -- test/client/lifecycle.test.ts test/client/resilience.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Every error kind has exact attempts, delay, reconnect, suspect, and context state.
    - Code 2 is one request with no retry/reconnect/delay.
    - Probe overrides affect only the request and never register counters.
    - No wall-clock or real endpoint participates.
  </acceptance_criteria>
  <done>The serialized lifecycle recovers and fails with exact Python-equivalent requests, controlled time, and normalized observable error state.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                        |
| ----------------------------------- | ---------------------------------------------------------------------------------- |
| Consumer concurrency → client state | Concurrent calls must not race connection or retry state.                          |
| Internal dependencies → public API  | Test injection must remain inaccessible from mapped declarations and root exports. |
| Normalized failure → retry policy   | Error kind controls reconnect, delay, and immediate failure.                       |

## STRIDE Threat Register

| Threat ID | Category               | Component                | Disposition | Mitigation Plan                                                          |
| --------- | ---------------------- | ------------------------ | ----------- | ------------------------------------------------------------------------ |
| T-02-01   | Denial of service      | configuration/retry loop | mitigate    | Validated finite attempts/group bounds and exact delays.                 |
| T-02-02   | Tampering / race       | FIFO and lifecycle       | mitigate    | One non-reentrant complete-operation gate with rejection recovery tests. |
| T-02-03   | Spoofing               | Code-2 branch            | mitigate    | Structured IllegalAddressError only.                                     |
| T-02-04   | Information disclosure | latest context           | mitigate    | Closed kind, endpoint redaction, no raw cause.                           |
| T-02-SC   | Tampering              | package supply chain     | accept      | No dependency installation occurs in this plan.                          |

No ASVS L1 high-severity item may remain open at plan completion.
</threat_model>

<artifacts_produced>

## Artifacts This Phase Produces

| Artifact                      | Purpose                                                     |
| ----------------------------- | ----------------------------------------------------------- |
| FIFO gate                     | Complete-operation serialization                            |
| internal client creation seam | Deterministic dependencies without public constructor drift |
| internal IdmModbusClient      | Lifecycle, retry, reconnect, probes, and error context      |
| lifecycle/resilience tests    | Exact defaults, ordering, attempts, delays, and state       |

</artifacts_produced>

<verification>
Run both focused commands, `npm run lint`, `npm run format:check`, and `npm run typecheck`. Generate declarations or inspect the source export closure to prove internal dependency names are absent.
</verification>

<success_criteria>

- All lifecycle and retry paths are serialized and deterministic.
- The public constructor remains exact and adapter-neutral without public injection fields.
- Exact attempts, reconnects, delays, probes, and contexts match pinned behavior.
- The class remains off the package root until adapter/read/detection evidence is complete.

</success_criteria>

<output>
Create `.planning/phases/02-modbus-reads-detection-and-resilience/02-05-SUMMARY.md` when done.
</output>
