---
phase: 01-reproducible-semantic-contract
plan: "09"
type: execute
wave: 7
depends_on:
  - 01-07
files_modified:
  - contracts/api-mapping.json
  - docs/API-PARITY.md
  - src/index.ts
  - test/parity/api-parity.test.ts
  - test/parity/phase-gate.test.ts
autonomous: true
requirements:
  - PAR-01
  - API-01
  - API-02
must_haves:
  truths:
    - "Only implemented and fully evidenced Phase-1 mapping rows move from planned to complete; Python-only class facts remain unchanged."
    - "After mapping edits, promotion uses guaranteed `npm run parity:api` to regenerate only API docs/inventory-derived API artifacts, followed by non-mutating `npm run parity:check` for Python fixtures; no full fixture generation, direct generator invocation, or upstream-directory variable is permitted."
    - "Exports are added only after promoted mapping/docs/contracts pass a clean hard check, and later-owned or incomplete rows remain planned and absent."
    - "Fixture files are verified byte-stable by the npm flows but are not listed as modified because no Python fact is expected to change."
  artifacts:
    - path: "contracts/api-mapping.json"
      provides: "Evidence-gated final Phase-1 lifecycle statuses and Node-only decisions"
    - path: "docs/API-PARITY.md"
      provides: "Regenerated mapping/status/member evidence matrix"
    - path: "src/index.ts"
      provides: "Explicit exports for verified complete Phase-1 rows only"
    - path: "test/parity/phase-gate.test.ts"
      provides: "Promotion-before-export state-machine and boundary tests"
  key_links:
    - from: "contracts/api-mapping.json"
      to: "test/fixtures/public-classes.json"
      via: "Node decisions validated against immutable Python-only facts"
      pattern: "representation|owner_phase|typescript_symbol"
    - from: "docs/API-PARITY.md"
      to: "contracts/api-mapping.json"
      via: "npm parity:api regeneration after status promotion"
      pattern: "complete|planned"
    - from: "src/index.ts"
      to: "contracts/api-mapping.json"
      via: "only checked complete root rows are exported"
      pattern: "export"
---

<objective>
Promote only proven Phase-1 API mappings, regenerate API-only artifacts and non-mutatingly check Python fixtures through guaranteed npm commands, then expose the exact verified root surface.

Purpose: Prevent status or export claims from preceding implementation, Python-fact closure, focused evidence, or reproducible contract checks.
Output: Final Phase-1 mapping statuses, regenerated API docs, evidence-gated root exports, and state-machine tests.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@contracts/api-mapping.json
@test/fixtures/public-api.json
@test/fixtures/public-classes.json
@docs/API-PARITY.md
@src/index.ts
@src/web/index.ts
@package.json
@test/parity/api-parity.test.ts
@.planning/phases/01-reproducible-semantic-contract/01-07-SUMMARY.md

<interfaces>
`public-classes.json` stays Python-only and is never edited by mapping promotion. `contracts/api-mapping.json` remains the sole authority for TypeScript names, export paths, owner phases, lifecycle status, evidence, normalizations, and representation decisions.

Promotion/check order is mandatory: focused evidence → eligible mapping status change → `npm run parity:api` for API docs/inventory-derived API artifacts only → non-mutating `npm run parity:check` for pinned Python fixtures and clean generated diff → root export. The npm commands are supplied by completed Plan 01-07 and require no ambient checkout variable; promotion never runs a full Python fixture-generation command.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-09-01: Promote only implemented and evidenced Phase-1 mapping rows</name>
  <files>contracts/api-mapping.json, docs/API-PARITY.md, test/parity/api-parity.test.ts, test/parity/phase-gate.test.ts</files>
  <read_first>
    - .planning/phases/01-reproducible-semantic-contract/01-07-SUMMARY.md
    - contracts/api-mapping.json
    - test/fixtures/public-api.json
    - test/fixtures/public-classes.json
    - docs/API-PARITY.md
    - src/constants.ts
    - src/types.ts
    - src/timing.ts
    - src/codec.ts
    - src/registers/index.ts
  </read_first>
  <behavior>
    - Mapping tests join Python-only class facts to mapping-only Node decisions and reject any Python fixture containing Node ownership/name/export/status/representation keys.
    - A Phase-1 row becomes complete only if implementation, complete public member/signature/default/validation closure, and focused passing evidence all exist.
    - Later-owned or incomplete rows remain planned; no partial or fabricated not-applicable status is used.
    - Guaranteed `parity:api` refreshes only API docs/inventory-derived API artifacts, while `parity:check` validates all six Python-derived fixtures non-mutatingly without treating those fixtures as task-modified artifacts.
  </behavior>
  <action>
Write state-machine and Python-facts-vs-Node-decisions RED tests. Run every focused semantic/codec/register evidence suite for owner-phase-1 planned rows. Promote only rows whose implementation and complete member/default/validation evidence pass; keep all others planned. After editing the mapping, run only `npm run parity:api` to regenerate API docs and inventory-derived API artifacts, then `npm run parity:check` with no `IDM_HEATPUMP_API_DIR` to validate pinned Python fixtures non-mutatingly. Require generated `docs/API-PARITY.md` to reflect the statuses and zero fixture diff because Python facts did not change. Never run full Python fixture generation, invoke Python/API generators directly, or edit `public-classes.json` or another fixture during promotion.
  </action>
  <verify>
    <automated>npm test -- test/parity/api-parity.test.ts test/semantic/constants-and-types.test.ts test/codec.test.ts test/registers &amp;&amp; npm run parity:api &amp;&amp; npm run parity:check &amp;&amp; git diff --exit-code -- test/fixtures</automated>
  </verify>
  <acceptance_criteria>
    - Complete statuses have actual implementation, full Python-fact closure, and passing focused evidence.
    - Node decisions remain mapping-only; Python fixtures remain byte-identical.
    - API docs/inventory-derived API artifacts are regenerated through `parity:api`, and the guaranteed non-mutating Python fixture check passes without ambient checkout state.
  </acceptance_criteria>
  <done>Mapping statuses and generated API documentation truthfully represent the implemented semantic core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-09-02: Export only the checked complete Phase-1 root surface</name>
  <files>src/index.ts, test/parity/api-parity.test.ts, test/parity/phase-gate.test.ts</files>
  <read_first>
    - contracts/api-mapping.json
    - docs/API-PARITY.md
    - src/index.ts
    - src/web/index.ts
    - src/constants.ts
    - src/types.ts
    - src/timing.ts
    - src/codec.ts
    - src/registers/index.ts
    - test/parity/phase-gate.test.ts
  </read_first>
  <behavior>
    - Every checked owner-phase-1 complete root row has exactly its mapped runtime/type export; no planned/partial/later-owned/web row is exported.
    - Web remains empty, later client/transport/write/web runtime stubs remain absent, and type-only exports are explicit.
    - The same source builds ESM/CJS/declarations, and a post-export `npm run parity:check` still passes.
  </behavior>
  <action>
Write export-boundary RED tests that require completed Task 01-09-01 evidence and reject export-before-check ordering. Add explicit `.js`-specifier root exports only for mapping rows now complete; use `export type` under `verbatimModuleSyntax`. Keep incomplete/later-owned rows absent and `src/web/index.ts` unchanged. Run build and API/export tests, then the guaranteed `npm run parity:check`; if any check fails, remove the export or return the mapping row to planned rather than introducing a stub.
  </action>
  <verify>
    <automated>npm test -- test/parity/api-parity.test.ts test/parity/phase-gate.test.ts -t "promotion|export|mapping" &amp;&amp; npm run build &amp;&amp; npm run parity:check</automated>
  </verify>
  <acceptance_criteria>
    - Mapping-to-export closure is exact for every complete Phase-1 root symbol.
    - Planned/later/web symbols cannot be imported.
    - ESM, CJS, declarations, and post-export parity check pass.
  </acceptance_criteria>
  <done>Consumers can import exactly the checked semantic core and no deceptive partial surface.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                              |
| ---------------------------------- | ---------------------------------------------------------------------------------------- |
| Python facts → Node mapping        | Python facts must not be contaminated with Node decisions or silently lose members.      |
| Mapping/evidence → complete status | A string evidence path or partial representation must not claim completion.              |
| Checked mapping → package barrel   | An export must not precede successful regeneration/check or expose later-owned behavior. |

## STRIDE Threat Register

| Threat ID | Category               | Component            | Disposition | Mitigation Plan                                                                               |
| --------- | ---------------------- | -------------------- | ----------- | --------------------------------------------------------------------------------------------- |
| T-1-01    | Spoofing               | fixture provenance   | mitigate    | Guaranteed exact-baseline npm flows from completed Plan 07.                                   |
| T-1-02    | Tampering              | mapping/docs/exports | mitigate    | Python/Node authority separation, focused evidence, regeneration/check, exact export closure. |
| T-1-03    | Repudiation            | completion claims    | mitigate    | Machine statuses, named evidence, generated docs, clean diffs, state-machine tests.           |
| T-1-04    | Information disclosure | public artifacts     | mitigate    | Public symbols/metadata only; no credentials, device data, PINs, IPs, or captures.            |
| T-1-05    | Denial of service      | mapping/class inputs | mitigate    | Closed bounded schemas, exact inventory counts, duplicate/unknown-key rejection.              |
| T-1-06    | Elevation of privilege | generators/package   | mitigate    | Only guaranteed shell-free npm flows; explicit fixed exports; no dynamic code or write path.  |
| T-1-SC    | Tampering              | package supply chain | accept      | No dependency changes in this plan; previously audited tooling only.                          |

</threat_model>

<artifacts_produced>

- Truthful final Phase-1 mapping statuses and regenerated API docs.
- Evidence-gated root exports.
- Promotion/export ordering and authority-separation tests.

</artifacts_produced>

<verification>
Run focused evidence, `npm run parity:api`, non-mutating `npm run parity:check`, API/export tests, build, and confirm fixtures have no diff.
</verification>

<success_criteria>

- Safe orchestration precedes promotion; promotion/check precedes export.
- Python-derived fixture facts remain separate and unchanged.
- Only checked complete Phase-1 rows are exported.

</success_criteria>

<output>
Create `.planning/phases/01-reproducible-semantic-contract/01-09-SUMMARY.md` when done.
</output>
