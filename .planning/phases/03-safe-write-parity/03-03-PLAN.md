---
phase: 03-safe-write-parity
plan: "03"
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/client/write-safety.ts
  - test/client/write-state.test.ts
  - test/client/write-validation.test.ts
  - test/parity/write-contract.test.ts
autonomous: true
requirements: [WRT-01, WRT-02]
must_haves:
  truths:
    - "WriteSafetyResult is deeply immutable, preserves exact register/requested-value identity, owns encoded words, and preserves its three intentional dry-run defaults."
    - "Planning follows the exact Python order and allowCustomRegister bypasses only detected-model name/address membership."
    - "EEPROM and cyclic state use injected monotonic seconds, exact boundaries, deterministic immutable projections, and success-only mutation hooks."
    - "Pure planning and state helpers cannot connect or send transport traffic."
  artifacts:
    - path: src/client/write-safety.ts
      provides: immutable result, ordered planner, and time-state authority
    - path: test/client/write-validation.test.ts
      provides: exact reason-order/custom/default evidence
    - path: test/client/write-state.test.ts
      provides: 60/300-second state and reset evidence
  key_links:
    - from: src/client/write-safety.ts
      to: src/codec.ts
      via: encodeValue only after ordered domain validation
      pattern: "encodeValue"
    - from: src/client/write-safety.ts
      to: src/registers/registry.ts
      via: model-aware lookup and exact name/address membership
      pattern: "getRegister|address"
---

<objective>
Implement Python-equivalent write planning and deterministic safety state as provider-neutral domain logic.

Purpose: Prove all acceptance/rejection and time boundaries without any real or fake Modbus execution.
Output: WriteSafetyResult, ordered planner, EEPROM/cyclic state authority, and focused/generated tests.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@src/codec.ts
@src/errors.ts
@src/registers/definitions.ts
@src/registers/registry.ts
@src/types.ts
@test/fixtures/write-behavior.json
@.planning/phases/03-safe-write-parity/03-CONTEXT.md
@.planning/phases/03-safe-write-parity/03-RESEARCH.md

<interfaces>
Public shapes are `WriteSafetyResult.create`, `simulateWrite(registerOrKey, value, options?)`, `writeRegister(register, value, options?)`, `setValue(key, value, options?)`, and four state methods. Factory default dryRun is false, simulation default true, and setValue default false. Plan 03-04 exercises these behaviors only through private internal controls; Plan 03-07 atomically attaches the seven public class methods, promotes their mappings, and exports the evidenced surface.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-03-01: Implement immutable result and exact ordered planner</name>
  <files>src/client/write-safety.ts, test/client/write-validation.test.ts, test/parity/write-contract.test.ts</files>
  <read_first>
    - src/codec.ts
    - src/errors.ts
    - src/registers/definitions.ts
    - src/registers/registry.ts
    - src/types.ts
    - test/fixtures/write-behavior.json
    - src/contracts/write-scenario.ts
  </read_first>
  <action>
Write RED tests, then add `WriteSafetyResultInput`, public `WriteSafetyResult` interface/frozen factory, internal `WritePlanInput`, and `createWritePlan`. Own/freeze encoded words and reject non-integer/out-of-range factory words while preserving the exact RegisterDef and requested-value references. Implement exact precedence: string lookup; read-only; if model exists then detected-map same name+address unless bypass; BOOL accepts only boolean or numeric 0/1; non-BOOL rejects boolean then non-number then non-finite; UCHAR/INT8/INT16/UINT16/BITFLAG require integer; `excludeFromWrite.has(Math.trunc(value))`; min; max; enum own key on `Math.trunc(value)`; EEPROM throttle; existing `encodeValue`. Never reject canonical writable input-register metadata—writes force holding later. Preserve no-model explicit custom acceptance and membership-only bypass. Add a CPython-binary64-compatible one-decimal remaining-time formatter; semantic equality remains the closed code.
  </action>
  <acceptance_criteria>
    - Every collision test returns the first authoritative code above.
    - Custom bypass cannot skip writable/type/finite/integer/excluded/range/enum/codec guards.
    - `simulateWrite` metadata false still describes a plan and has no transport dependency.
    - Float32 `42.5` produces frozen `[0, 16938]`; all generated validation cases match.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/client/write-validation.test.ts test/parity/write-contract.test.ts -t "validation|plan|custom|default|identity" &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>TypeScript accepts/rejects and plans exactly like the pinned Python domain without transport.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-03-02: Implement EEPROM and cyclic state boundaries</name>
  <files>src/client/write-safety.ts, test/client/write-state.test.ts, test/parity/write-contract.test.ts</files>
  <read_first>
    - src/client/write-safety.ts
    - test/support/fake-clock.ts
    - src/contracts/canonical-order.ts
    - test/fixtures/write-behavior.json
  </read_first>
  <action>
Add internal `WriteSafetyState`, `InternalWriteSafetyStateSeed`, and `InternalWriteSafetyStateSnapshot`. Store private `Map&lt;string, number&gt;` instances. `assertEepromWriteAllowed` blocks elapsed `< 60` and reports one-decimal remaining time; exact 60 succeeds. `recordSuccessfulWrite(register, now)` alone records EEPROM `now` or cyclic `now + (cyclicWriteTtl ?? 300)`. Add scoped/global `resetWriteThrottle` and `resetCyclicWriteState`, sorted frozen active records for `deadline > now`, immutable set-like expired names for `deadline <= now`, and internal seed/snapshot helpers. Build records via sorted Map entries and `Object.fromEntries` so `__proto__` is safe. Never mutate on planning/rejection and never expose remaining TTL.
  </action>
  <acceptance_criteria>
    - 59.999 seconds blocks and exactly 60 allows; names throttle independently.
    - Default 300/custom TTL, refresh, exact-deadline expiry, and resets match generated cases.
    - Returned records/set-like views cannot be mutated and are Unicode-code-point ordered.
    - Failed/rejected/dry-run hooks leave prior timestamps/deadlines byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/client/write-state.test.ts test/client/write-validation.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Time-sensitive write safety is deterministic, immutable, and ready for one client-owned commit point.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts This Phase Produces

| New symbol/path                                                     | Purpose                                |
| ------------------------------------------------------------------- | -------------------------------------- |
| `src/client/write-safety.ts`                                        | Provider-neutral write domain          |
| `WriteSafetyResultInput` / `WriteSafetyResult`                      | Exact immutable public result shape    |
| `WriteSafetyResult.create`                                          | Factory with dryRun false default      |
| `WritePlanInput` / `createWritePlan`                                | Ordered Python-equivalent planner      |
| `WriteSafetyState`                                                  | EEPROM/cyclic private state machine    |
| `InternalWriteSafetyStateSeed` / `InternalWriteSafetyStateSnapshot` | Deterministic internal test state      |
| `test/client/write-validation.test.ts`                              | Acceptance/reason/default/custom suite |
| `test/client/write-state.test.ts`                                   | Controlled-time state suite            |

</artifacts_produced>

<verification>
Run both task commands, lint, format check, and diff check. Confirm no transport/adapter call, root export, mapping promotion, register edit, or hardware access.
</verification>

<success_criteria>

- WRT-01 and WRT-02 pure-domain behavior matches the generated authority.
- All later execution can use one immutable plan and one success-only state method.
- The package is still private and incomplete.
  </success_criteria>

<output>Create `.planning/phases/03-safe-write-parity/03-03-SUMMARY.md` when done.</output>
