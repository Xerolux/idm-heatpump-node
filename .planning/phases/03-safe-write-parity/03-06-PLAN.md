---
phase: 03-safe-write-parity
plan: "06"
type: execute
wave: 6
depends_on: [03-02, 03-04, 03-05]
files_modified:
  - src/contracts/write-scenario.ts
  - test/parity/generator.test.ts
  - test/parity/write-contract.test.ts
  - test/support/write-scenario-runner.ts
autonomous: true
requirements: [WRT-01, WRT-02, TRN-03W, ERR-01W]
must_haves:
  truths:
    - "Every generated Python write scenario is parsed and executed exactly once by the TypeScript client."
    - "Results/errors, ordered FC16 requests, controlled clock, retries/reconnects, EEPROM/cyclic state, and diagnostics compare structurally."
    - "Mutation, secrecy, graph bounds, transaction rollback, and fixed-point regeneration fail closed."
    - "Parity closure uses no handwritten expected write behavior, live TCP, or hardware write."
  artifacts:
    - path: test/support/write-scenario-runner.ts
      provides: independent deterministic TypeScript scenario executor
    - path: test/parity/write-contract.test.ts
      provides: complete Python-to-TypeScript behavioral comparison
    - path: test/parity/generator.test.ts
      provides: mutation, rollback, provenance, and fixed-point closure
  key_links:
    - from: test/fixtures/write-behavior.json
      to: test/support/write-scenario-runner.ts
      via: parseWriteBehaviorFixture before execution
      pattern: "parseWriteBehaviorFixture"
    - from: test/support/write-scenario-runner.ts
      to: src/client/idm-modbus-client.ts
      via: private internal write controls with hidden fake clock/transport creation only
      pattern: "simulateInternalWrite|writeInternalRegister|setInternalValue"
---

<objective>
Close the executable Python/TypeScript write contract before any public mapping or export is promoted.

Purpose: Turn the generated fixture into complete behavioral evidence across results, traffic, time, state, retries, and secrecy.
Output: Independent runner, exhaustive parity suite, and hardened transactional evidence tests.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@UPSTREAM-PARITY.json
@src/contracts/write-scenario.ts
@src/client/internal-create.ts
@test/fixtures/write-behavior.json
@test/support/fake-clock.ts
@test/support/fake-modbus-transport.ts
@test/parity/generator.test.ts
@test/parity/write-contract.test.ts
@.planning/phases/03-safe-write-parity/03-RESEARCH.md

<interfaces>
The runner consumes only the closed parsed fixture. It must assert all eight CTR fields, exact action order, exact request order, and complete post-action state. Fixture expectations remain Python-owned.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-06-01: Execute every generated scenario exactly once</name>
  <files>src/contracts/write-scenario.ts, test/parity/write-contract.test.ts, test/support/write-scenario-runner.ts</files>
  <read_first>
    - src/contracts/write-scenario.ts
    - test/fixtures/write-behavior.json
    - src/client/idm-modbus-client.ts
    - src/client/internal-create.ts
    - test/support/fake-clock.ts
    - test/support/fake-modbus-transport.ts
    - test/parity/transport-contract.test.ts
  </read_first>
  <action>
Write the comparison tests first, then add test-only `executeWriteBehaviorScenario` and `executeWriteBehaviorFixture`. Construct only internal clients using `example.invalid`, controlled `FakeClock`, and scripted `FakeModbusTransport`; build custom RegisterDefs through the real factory; dispatch the exact eight action kinds; project errors to `{category,code}` or closed transport kinds; snapshot results, requests, clock observations, connection/suspect/error state, EEPROM timestamps, cyclic deadlines, and unsupported/permanent state using the existing normalizers. Assert the parsed scenario-name set equals the executed-name set with no duplicate/skip, every response is consumed, and every observable field equals Python. Include exact FC16 one-/two-word order, dry-run no traffic, 60/300 boundaries, retries/reconnects, Code 2, rollback, FIFO, and last-error persistence.
  </action>
  <acceptance_criteria>
    - All generated scenarios run once and only once.
    - Exact result/error/request/clock/state equality holds for every action boundary.
    - The runner contains no expected addresses/words/outcomes not sourced by the fixture.
    - No socket/provider/default transport is created.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/parity/write-contract.test.ts &amp;&amp; npm run parity:check</automated>
  </verify>
  <done>All four Phase-3 requirements have independent executable cross-language evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-06-02: Close mutation, secrecy, rollback, and fixed-point evidence</name>
  <files>src/contracts/write-scenario.ts, test/parity/generator.test.ts, test/parity/write-contract.test.ts, test/support/write-scenario-runner.ts</files>
  <read_first>
    - src/contracts/write-scenario.ts
    - test/parity/generator.test.ts
    - test/parity/write-contract.test.ts
    - scripts/check-parity.mjs
    - scripts/generate-python-contract.py
  </read_first>
  <action>
Add adversarial mutations for every closed action/result/error/request/state branch, duplicate IDs, dangerous keys, word/count/address bounds, exceptional-number tags, excess bytes/depth/nodes/actions, missing snapshots, unredacted endpoint, raw cause/provider payload leakage, and partial transactions. The closed fixture is intentionally allowed to contain synthetic action values and `expected_requests[].words`, because those are required parity evidence. Scope secrecy scans to errors, diagnostics, logs, generated documentation, packed artifacts, and raw provider payload/cause projections; reject real endpoint/IP data and any requested value/encoded words appearing in those forbidden sinks. Do not reject the reviewed synthetic values/words in action inputs or expected FC16 traces. Force generation failure after staged/replaced write output and prove all ten artifacts restore byte-identically. Regenerate, run check twice, and require zero governed diff. Do not weaken limits or use message equivalence.
  </action>
  <acceptance_criteria>
    - Every untrusted mutation fails with a closed parser/generator error.
    - Synthetic action values and expected FC16 words remain in their closed fixture fields, while errors, diagnostics, logs, docs, package output, and raw provider projections contain none of them.
    - Failed replacement restores all ten artifacts; generate/check is a fixed point.
    - The seven old fixtures remain byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/parity/write-contract.test.ts test/parity/generator.test.ts -t "mutation|redact|leak|rollback|fixed point" &amp;&amp; npm run parity:generate &amp;&amp; npm run parity:check &amp;&amp; git diff --exit-code -- test/fixtures docs/API-PARITY.md docs/BASELINE.md</automated>
  </verify>
  <done>Generated parity is complete, confidential, bounded, atomic, and stable enough for API promotion.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts This Phase Produces

| New symbol/path                         | Purpose                                  |
| --------------------------------------- | ---------------------------------------- |
| `test/support/write-scenario-runner.ts` | Independent no-network TS executor       |
| `executeWriteBehaviorScenario`          | One closed scenario dispatcher/projector |
| `executeWriteBehaviorFixture`           | Exact inventory execution entry point    |
| complete write parity assertions        | Result/request/time/state equality       |
| write mutation/secrecy/rollback cases   | Fail-closed evidence hardening           |

</artifacts_produced>

<verification>
Run both task commands, lint, format, typecheck, diff check, and confirm no mapping/root/package promotion occurred.
</verification>

<success_criteria>

- WRT-01, WRT-02, TRN-03W, and ERR-01W have complete generated executable evidence.
- Old read evidence remains stable.
- Public promotion is now dependency-safe but has not yet happened.
  </success_criteria>

<output>Create `.planning/phases/03-safe-write-parity/03-06-SUMMARY.md` when done.</output>
