---
phase: 03-safe-write-parity
plan: "07"
type: execute
wave: 7
depends_on: [03-06]
files_modified:
  - CHANGELOG.md
  - README.md
  - contracts/api-mapping.json
  - docs/API-PARITY.md
  - scripts/check-package.mjs
  - src/client/idm-modbus-client.ts
  - src/client/internal-create.ts
  - src/client/index.ts
  - src/index.ts
  - test/parity/api-parity.test.ts
  - test/parity/phase-gate.test.ts
autonomous: true
requirements: [WRT-01, WRT-02, TRN-03W, ERR-01W]
must_haves:
  truths:
    - "Only after generated evidence, WriteSafetyResult and the now-29-member IdmModbusClient become complete and explicitly exported."
    - "ESM, CommonJS, declarations, and clean tarball consumers expose exact planning/dry-run/state methods without internal seams or hardware access."
    - "The phase gate closes the four Phase-3 child requirements and makes TRN-03/ERR-01 umbrella completion eligible while CTR-02/PAR-02/web/release remain pending."
    - "The package stays private, publication/latest-stable claims stay blocked, and release-mode mapping generation still fails."
    - "Documentation states bounded at-least-once retry risk and explicitly claims no live Node hardware validation."
  artifacts:
    - path: contracts/api-mapping.json
      provides: exact complete WriteSafetyResult/client mappings
    - path: src/index.ts
      provides: evidence-gated write exports
    - path: scripts/check-package.mjs
      provides: clean ESM/CJS/declaration dry-run/state smoke
    - path: test/parity/phase-gate.test.ts
      provides: complete private Phase-3 closure gate
    - path: README.md
      provides: truthful safe-write usage and operational limits
  key_links:
    - from: contracts/api-mapping.json
      to: src/index.ts
      via: explicit complete mapping-authorized exports
      pattern: "IdmModbusClient|WriteSafetyResult"
    - from: test/parity/phase-gate.test.ts
      to: .planning/REQUIREMENTS.md
      via: child/umbrella completion and release exclusions
      pattern: "WRT-01|WRT-02|TRN-03W|ERR-01W|CTR-02|PAR-02"
---

<objective>
Promote the fully evidenced write surface and close the complete private Phase-3 quality/package/documentation gate without releasing.

Purpose: Make safe writes usable to private-package consumers while preventing any false web, latest-stable, hardware, or npm-publication claim.
Output: Complete mappings/exports, clean tarball smoke, truthful docs, and full phase gate.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@UPSTREAM-PARITY.json
@package.json
@contracts/api-mapping.json
@docs/API-PARITY.md
@src/client/index.ts
@src/index.ts
@scripts/check-package.mjs
@test/parity/api-parity.test.ts
@test/parity/phase-gate.test.ts
@README.md
@CHANGELOG.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-safe-write-parity/03-VALIDATION.md

<interfaces>
After promotion the mapping inventory is exactly 59 complete, 30 planned, zero partial, 89 total Python rows. `IdmModbusClient` has all 29 normalized members and no `partial_class`. `WriteSafetyResult` is complete. The package remains `private:true`; web/release rows remain planned.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-07-01: Promote exact mappings, exports, declarations, and clean package smoke</name>
  <files>contracts/api-mapping.json, docs/API-PARITY.md, scripts/check-package.mjs, src/client/idm-modbus-client.ts, src/client/internal-create.ts, src/client/index.ts, src/index.ts, test/parity/api-parity.test.ts, test/parity/phase-gate.test.ts</files>
  <read_first>
    - contracts/api-mapping.json
    - test/fixtures/public-classes.json
    - test/fixtures/write-behavior.json
    - src/client/write-safety.ts
    - src/client/idm-modbus-client.ts
    - src/client/index.ts
    - src/index.ts
    - scripts/generate-api-parity.mjs
    - scripts/check-package.mjs
    - test/parity/api-parity.test.ts
  </read_first>
  <action>
Write RED status/member/export/declaration/package tests. Only after rerunning all focused generated write evidence, add the seven public `IdmModbusClient` methods as thin wrappers over the already-proven private helpers: synchronous `simulateWrite`, four synchronous query/reset methods, and FIFO-backed async `writeRegister`/`setValue`. Keep synchronous reset-vs-in-flight behavior unchanged. Then change `WriteSafetyResult` from planned to complete; change `IdmModbusClient` from partial to complete and remove `partial_class`; retain its owner phase and evidence path. Assert exactly 59 complete/30 planned/0 partial/89 total and all 29 normalized fixture members. Regenerate `docs/API-PARITY.md`. Explicitly export `WriteSafetyResult` from client/root using named `.js` exports; expose no planner/state/internal factory/adapter/provider symbols. Extend clean installed ESM/CJS/TypeScript smoke to instantiate the result, run `simulateWrite`, `setValue(...,{dryRun:true})`, and state queries/resets without connect; verify exact declarations/options/defaults and no internal injection. Keep tarball allowlist and dependency pin exact.
  </action>
  <acceptance_criteria>
    - Mapping counts and 29-member class equal the pinned fixture exactly.
    - Root runtime/declaration exports add only the mapped WriteSafetyResult and completed methods.
    - Clean package smoke plans/dry-runs/queries without network or state success mutation.
    - Release-mode generation remains red due only to later planned web/release rows.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/parity/api-parity.test.ts test/parity/phase-gate.test.ts &amp;&amp; npm run parity:api &amp;&amp; npm run parity:check &amp;&amp; npm run build &amp;&amp; npm run pack:check</automated>
  </verify>
  <done>The exact evidenced safe-write API is usable from the private package with no internal leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-07-02: Close truthful documentation and the full private Phase-3 gate</name>
  <files>CHANGELOG.md, README.md, test/parity/phase-gate.test.ts</files>
  <read_first>
    - README.md
    - CHANGELOG.md
    - AGENTS.md
    - UPSTREAM-PARITY.json
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/03-safe-write-parity/03-VALIDATION.md
    - contracts/api-mapping.json
    - test/parity/phase-gate.test.ts
  </read_first>
  <action>
Write RED documentation and closure assertions. Document exact baseline version/tag/full SHA; plan versus real write examples; FC16 for one/two words; validation/custom-membership-only bypass; EEPROM 60 seconds; cyclic default 300 seconds; fake-clock/no-live-write evidence; trusted-network/no TLS/auth boundary; bounded at-least-once ambiguity when a response is lost; Navigator 1.x exclusion; and web/publication/latest-stable/hardware validation still pending. Update Unreleased changelog with only proven Phase-3 behavior.

Make the gate assert eight fixtures/two docs/ten artifacts, complete generated scenarios, 59/30/0 mappings, 29 methods, exact root/tarball surface, package private, modbus-serial 8.0.25, no secrets/payloads/internal seams, WRT-01/WRT-02/TRN-03W/ERR-01W evidence, and child-clause eligibility for TRN-03/ERR-01. It must reject completing CTR-02, PAR-02, web/release requirements, removing `private`, claiming latest stable, claiming live hardware, or successful `generate-api-parity --release`. Run full check/parity/audit/package gates and governed clean diff.
</action>
<acceptance_criteria> - Full unit/integration/parity/coverage/build/package/audit gates pass. - Documentation has no publication, latest-stable, exactly-once, TLS/auth, web, or hardware overclaim. - Phase-3 requirements have evidence while Phase 4/5 remain fail-closed. - No register map, dependency, live endpoint, or hardware interaction changed.
</acceptance_criteria>
<verify>
<automated>npm run check &amp;&amp; npm run parity:check &amp;&amp; npm audit --omit=dev &amp;&amp; ! node scripts/generate-api-parity.mjs --release &amp;&amp; git diff --exit-code -- contracts docs/API-PARITY.md docs/BASELINE.md test/fixtures</automated>
</verify>
<done>Phase 3 is fully evidenced, packaged, documented, private, and ready for independent verification.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts This Phase Produces

| New symbol/path                              | Purpose                                  |
| -------------------------------------------- | ---------------------------------------- |
| complete `WriteSafetyResult` mapping         | Public immutable result authority        |
| complete 29-member `IdmModbusClient` mapping | Full Modbus read/write client authority  |
| root/client `WriteSafetyResult` exports      | Explicit public runtime/type surface     |
| clean dry-run/state tarball smoke            | No-network consumer proof                |
| Phase-3 README write section                 | Safe usage and operational risk guidance |
| Unreleased Phase-3 changelog entry           | Exact delivered behavior/baseline record |
| complete private Phase-3 gate                | Quality/parity/privacy/release exclusion |

</artifacts_produced>

<verification>
Run both task commands, `npm ls modbus-serial --depth=0`, lint/format/typecheck, and diff check. Confirm package remains private and no npm publish command runs.
</verification>

<success_criteria>

- WRT-01, WRT-02, TRN-03W, and ERR-01W are closable from executable evidence.
- TRN-03 and ERR-01 have both read and write child evidence.
- CTR-02/PAR-02/web/release remain pending and publication remains impossible.
  </success_criteria>

<output>Create `.planning/phases/03-safe-write-parity/03-07-SUMMARY.md` when done.</output>
