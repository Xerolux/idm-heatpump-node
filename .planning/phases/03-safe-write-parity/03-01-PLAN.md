---
phase: 03-safe-write-parity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/normalization.md
  - src/errors.ts
  - src/transport/types.ts
  - test/client/errors.test.ts
  - test/support/fake-modbus-transport.ts
  - test/transport/write-request.test.ts
autonomous: true
requirements: [WRT-01, WRT-02, TRN-03W, ERR-01W]
must_haves:
  truths:
    - "Write validation failures use a reviewed finite semantic code set, never message matching or JavaScript coercion."
    - "Every accepted one- or two-word write is representable only as holding-register FC16 with exact address, count, and owned word order."
    - "Invalid unit IDs, addresses, spans, counts, and words fail before any transport method can run."
    - "The fake transport records immutable FC16 events and never opens a network connection."
    - "Wave 1 adds write capability through a narrow ModbusWriteTransport refinement, so every existing read-only ModbusTransport implementer still typechecks unchanged."
  artifacts:
    - path: contracts/normalization.md
      provides: closed write-validation normalization authority
    - path: src/transport/types.ts
      provides: immutable provider-neutral FC16 request and temporary write-capable transport refinement
    - path: test/support/fake-modbus-transport.ts
      provides: deterministic write success/error and concurrency seam
    - path: test/transport/write-request.test.ts
      provides: bounded request and fake-event proof
  key_links:
    - from: src/transport/types.ts
      to: test/support/fake-modbus-transport.ts
      via: createModbusWriteRequest revalidation
      pattern: "functionCode: 16|registerType: RegisterType.HOLDING"
---

<objective>
Establish the fail-closed semantic and adapter-neutral foundation for every Phase-3 write before client or provider execution exists.

Purpose: Make wrong function codes, payload shapes, unsafe values, and circular test evidence unrepresentable at the first boundary.
Output: Closed write codes, frozen FC16 request factory, deterministic fake writes, and focused tests.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@UPSTREAM-PARITY.json
@contracts/normalization.md
@src/errors.ts
@src/transport/types.ts
@test/support/fake-modbus-transport.ts
@.planning/phases/03-safe-write-parity/03-CONTEXT.md
@.planning/phases/03-safe-write-parity/03-RESEARCH.md
@.planning/phases/03-safe-write-parity/03-VALIDATION.md

<interfaces>
The pinned authority is Python 0.7.6, tag v0.7.6, SHA ad121ebf34a5f5e37204371c026927d77efcd15c. Every real write is FC16, including one-word values. Modbus FC16 permits 1..123 words, unit 1..247, address 0..65535, and no span beyond address 65535.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-01-01: Close write validation codes and the immutable FC16 request</name>
  <files>contracts/normalization.md, src/errors.ts, src/transport/types.ts, test/client/errors.test.ts, test/transport/write-request.test.ts</files>
  <read_first>
    - contracts/normalization.md
    - src/errors.ts
    - src/transport/types.ts
    - test/client/errors.test.ts
    - src/registers/definitions.ts
    - src/codec.ts
  </read_first>
  <action>
Write RED tests, then add exactly these semantic codes to the reviewed normalization table and `SemanticValidationErrorCode`: `write_unknown_register`, `write_read_only`, `write_model_unavailable`, `write_boolean_required`, `write_boolean_for_numeric`, `write_not_numeric`, `write_nonfinite`, `write_integer_required`, `write_excluded`, `write_below_minimum`, `write_above_maximum`, `write_enum_unsupported`, and `write_eeprom_throttled`. Do not add a catch-all write code.

In `src/transport/types.ts`, add `MODBUS_WRITE_LIMITS`, `ModbusWriteRequestInput`, `ModbusWriteRequest`, and `createModbusWriteRequest`. The factory must accept exactly unit ID, address, count, words, `registerType: "holding"`, and `functionCode: 16`; require unit 1..247, address 0..65535, count 1..123, `count === words.length`, an in-range complete span, and integer words 0..65535; copy/freeze words and freeze the outer request. Add temporary `ModbusWriteTransport extends ModbusTransport` with `write(request): Promise&lt;void&gt;`; do not add `write` to the already-public base `ModbusTransport` in this wave because its existing real and test implementers are still read-only. Assert one-word FC16, Float32 `[0, 16938]` at `1696/count=2`, mutation resistance, rejected FC3/FC4/FC6, and every numeric boundary. Do not change read limits or read behavior.
</action>
<acceptance_criteria> - The normalization document and TypeScript union contain the same exact 13 write codes. - A valid request is deeply immutable and preserves low-word-first order. - Count zero, count 124, mismatched length, invalid words, and an overflowing address span reject deterministically. - Existing read request and error tests remain green.
</acceptance_criteria>
<verify>
<automated>npm test -- test/transport/write-request.test.ts test/client/errors.test.ts &amp;&amp; npm run typecheck</automated>
</verify>
<done>The domain has one reviewed write-error vocabulary and one exact FC16 request shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-01-02: Extend the fake transport with bounded FC16 events</name>
  <files>test/support/fake-modbus-transport.ts, test/transport/write-request.test.ts</files>
  <read_first>
    - test/support/fake-modbus-transport.ts
    - test/support/fake-clock.ts
    - src/transport/types.ts
    - test/transport/write-request.test.ts
  </read_first>
  <action>
Add `FakeModbusWriteResponse` (`write_ok` or structured `error`), make `FakeModbusTransport` satisfy the narrow `ModbusWriteTransport` refinement, and add a `FakeModbusTransportEvent` write arm carrying the exact frozen request. Revalidate with `createModbusWriteRequest`, consume exactly one scripted outcome per write, maintain the shared active/max-active counters, and add independently controllable paused writes (`pauseWrites`, `pendingWrites`, `waitForPendingWrites`, `releaseNextWrite`, `releaseAllWrites`) without weakening existing read pause behavior. A fake write must reject while disconnected, reject an exhausted script, never infer an acknowledgement, and never contact TCP. Existing `ModbusTransport` implementers require no edits in this wave.
  </action>
  <acceptance_criteria>
    - Successful and failed writes record one immutable event in exact order.
    - Fake responses are copied/closed and scripts must be consumed exactly.
    - Read-only tests remain byte- and behavior-compatible.
    - Paused writes expose deterministic concurrency without timers or network access.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/transport/write-request.test.ts -t "fake|FC16|word|count" &amp;&amp; npm test -- test/client/reads.test.ts test/client/lifecycle.test.ts</automated>
  </verify>
  <done>Later client tests can prove exact write traffic and FIFO behavior entirely in memory.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts This Phase Produces

| New symbol/path                                  | Purpose                                             |
| ------------------------------------------------ | --------------------------------------------------- |
| `SemanticValidationErrorCode` write arms         | Exact domain rejection vocabulary                   |
| `MODBUS_WRITE_LIMITS`                            | FC16 protocol bounds                                |
| `ModbusWriteRequestInput` / `ModbusWriteRequest` | Provider-neutral write type                         |
| `createModbusWriteRequest`                       | Deeply immutable validated request factory          |
| `ModbusWriteTransport`                           | Temporary narrow write-capable transport refinement |
| `FakeModbusWriteResponse`                        | Deterministic fake outcome union                    |
| `FakeModbusTransport.write` and write event arm  | Exact no-network trace                              |
| `test/transport/write-request.test.ts`           | Request/fake contract suite                         |

</artifacts_produced>

<verification>
Run both task commands, `npm run lint`, `npm run format:check`, and `npm run typecheck`. Confirm `git diff --check` and no register-schema/package-lock change.
</verification>

<success_criteria>

- WRT-01/WRT-02 rejection vocabulary and TRN-03W request identity are machine-closed.
- ERR-01W receives no permissive fallback code.
- No real adapter, public export, live network, register map, or release state changes.
  </success_criteria>

<output>Create `.planning/phases/03-safe-write-parity/03-01-SUMMARY.md` when done.</output>
