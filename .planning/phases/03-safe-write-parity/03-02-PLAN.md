---
phase: 03-safe-write-parity
plan: "02"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - scripts/check-parity.mjs
  - scripts/generate-python-contract.py
  - src/contracts/write-scenario.ts
  - test/fixtures/write-behavior.json
  - test/parity/generator.test.ts
  - test/parity/write-contract.test.ts
autonomous: true
requirements: [WRT-01, WRT-02, TRN-03W, ERR-01W]
must_haves:
  truths:
    - "The exact pinned Python checkout, not TypeScript, produces every expected write result, request trace, controlled-time transition, and state snapshot."
    - "Write behavior uses a separate closed fixture and leaves all seven pre-Phase-3 fixture bytes unchanged."
    - "Generation remains one atomic ten-artifact transaction with rollback, bounds, symlink rejection, and fixed-point check mode."
    - "Untrusted fixture graphs, actions, custom definitions, errors, and FC16 requests fail closed before execution."
  artifacts:
    - path: src/contracts/write-scenario.ts
      provides: bounded closed write-fixture parser
    - path: test/fixtures/write-behavior.json
      provides: exact Python-generated write authority
    - path: scripts/generate-python-contract.py
      provides: controlled Python write scenario generation
    - path: scripts/check-parity.mjs
      provides: eight-fixture/two-document transaction
  key_links:
    - from: scripts/generate-python-contract.py
      to: test/fixtures/write-behavior.json
      via: exact clean pinned checkout execution
      pattern: "v0.7.6|ad121ebf34a5f5e37204371c026927d77efcd15c"
    - from: src/contracts/write-scenario.ts
      to: src/transport/types.ts
      via: every expected request passes createModbusWriteRequest
      pattern: "functionCode.*16"
---

<objective>
Create the independent generated write authority before implementing or promoting the TypeScript write API.

Purpose: Prevent handwritten/circular parity claims while preserving the already-proven Phase-2 read contract byte-for-byte.
Output: Closed parser, eighth fixture, exact Python harness, and atomic ten-artifact generation.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@UPSTREAM-PARITY.json
@docs/PARITY-CONTRACT.md
@contracts/normalization.md
@scripts/check-parity.mjs
@scripts/generate-python-contract.py
@src/contracts/transport-scenario.ts
@test/parity/generator.test.ts
@.planning/phases/03-safe-write-parity/03-RESEARCH.md

<interfaces>
The new fixture retains the eight CTR fields `name`, `configuration`, `transport_responses`, `clock`, `operation`, `expected_result`, `expected_requests`, and `expected_state`. Its closed action kinds are `simulate_write`, `write_register`, `set_value`, `advance_time`, `reset_write_throttle`, `get_active_cyclic_writes`, `get_expired_cyclic_writes`, and `reset_cyclic_write_state`.

The repository's executable parity runner is `scripts/check-parity.mjs` (this tree has no `scripts/run-parity.mjs`). It self-provisions an owned checkout and detaches it at the manifest SHA before Python execution. Ambient upstream working-tree files are never authority.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-02-01: Define and mutation-test the closed write scenario contract</name>
  <files>src/contracts/write-scenario.ts, test/parity/write-contract.test.ts</files>
  <read_first>
    - src/contracts/transport-scenario.ts
    - src/contracts/scenario.ts
    - src/contracts/tagged-values.ts
    - src/transport/types.ts
    - test/parity/transport-contract.test.ts
    - contracts/normalization.md
  </read_first>
  <action>
Write RED parser/mutation tests, then create `src/contracts/write-scenario.ts` with exported `WriteScenarioAction`, `WriteBehaviorScenario`, `WriteBehaviorFixture`, and `parseWriteBehaviorFixture`. Reuse the tagged-number parser, canonical object-key guards, and real RegisterDef/FC16 factories. Require exact baseline identity and schema/generator version 1, the eight action kinds above, unique scenario names, `example.invalid`, all eight CTR fields, and exact action/result/request/state keys. Bound source bytes, depth, nodes, text, scenarios, actions, responses, clock entries, word count, and diagnostic length at constants no weaker than the existing transport parser. Reject prototype keys, extra/missing fields, illegal words/counts, raw causes/payloads, endpoints other than the synthetic endpoint, and unknown error codes/kinds. Parsing must never execute an action.
  </action>
  <acceptance_criteria>
    - A minimal canonical synthetic fixture parses to a deeply immutable graph.
    - Every missing/extra key, dangerous key, bound overflow, illegal tag, and malformed FC16 trace rejects.
    - The parser imports no client/provider implementation and opens no file/network connection.
    - Existing transport fixture/parser tests are unchanged and green.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/parity/write-contract.test.ts -t "schema|parser|bound|mutation" &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>The future generated file has a closed independently testable trust boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-02-02: Generate the complete pinned Python write matrix transactionally</name>
  <files>scripts/generate-python-contract.py, scripts/check-parity.mjs, test/fixtures/write-behavior.json, test/parity/generator.test.ts, test/parity/write-contract.test.ts</files>
  <read_first>
    - scripts/generate-python-contract.py
    - scripts/check-parity.mjs
    - test/parity/generator.test.ts
    - test/fixtures/transport-behavior.json
    - src/contracts/write-scenario.ts
    - scripts/check-parity.mjs, especially owned checkout creation and detached SHA verification
    - `git -C /mnt/c/Users/basti/Documents/GitHub/idm-heatpump-api show ad121ebf34a5f5e37204371c026927d77efcd15c:idm_heatpump/client.py`
    - `git -C /mnt/c/Users/basti/Documents/GitHub/idm-heatpump-api show ad121ebf34a5f5e37204371c026927d77efcd15c:tests/test_client.py`
    - `git -C /mnt/c/Users/basti/Documents/GitHub/idm-heatpump-api show ad121ebf34a5f5e37204371c026927d77efcd15c:tests/test_value_boundaries.py`
  </read_first>
  <action>
Extend the isolated Python runtime harness with controlled monotonic `_time`, fake `write_registers`, exact FC16 trace recording, scripted acknowledgements/errors, and sequence actions. Generate only through `scripts/check-parity.mjs` after it creates and verifies its owned detached checkout at `ad121ebf34a5f5e37204371c026927d77efcd15c`; the three `git show` objects above are review authority, and the ambient upstream checkout HEAD/content must never be imported as evidence. Generate the complete research matrix: plan/default/identity/no-traffic, lookup/writable/model/custom bypass, BOOL/numeric/finite/integer, excluded/min/max/enum/codec precedence, one-/two-word requests, EEPROM 60-second boundaries/resets, cyclic 300/custom TTL/refresh/expiry/resets, retry/reconnect/Code-2/exhaustion, rollback, diagnostics secrecy, and FIFO ordering. Source validation codes from explicit reviewed scenario cases, never message matching.

Add `test/fixtures/write-behavior.json` to both fixed allowlists. Change governed labels to exactly eight fixtures, two documents, and ten generated artifacts. Preserve canonical staging/reparse, size checks, directory closure, symlink/escape rejection, fsync/replacement, injected partial-replacement rollback, and check mode. In tests snapshot all seven old fixture bytes before generation and assert they are identical afterward; assert a forced failure restores all ten files; assert successful generate then check is a fixed point. Do not modify API mapping yet.
</action>
<acceptance_criteria> - Fixture baseline is exactly Python 0.7.6/v0.7.6/full SHA and every scenario name is unique. - Required generated scenario categories and intermediate states are all present. - Old fixture bytes are unchanged and the new fixture parses through `parseWriteBehaviorFixture`. - Failure rollback and non-mutating check govern all ten artifacts.
</acceptance_criteria>
<verify>
<automated>npm run parity:generate &amp;&amp; npm run parity:check &amp;&amp; npm test -- test/parity/generator.test.ts test/parity/write-contract.test.ts -t "write|ten|rollback|fixed point|old fixture"</automated>
</verify>
<done>The repository contains non-circular executable Python write evidence before TypeScript API promotion.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts This Phase Produces

| New symbol/path                                  | Purpose                                |
| ------------------------------------------------ | -------------------------------------- |
| `src/contracts/write-scenario.ts`                | Separate write evidence trust boundary |
| `WriteScenarioAction`                            | Closed eight-action union              |
| `WriteBehaviorScenario` / `WriteBehaviorFixture` | Parsed immutable fixture types         |
| `parseWriteBehaviorFixture`                      | Bounded parser entry point             |
| `test/fixtures/write-behavior.json`              | Eighth Python-generated fixture        |
| Python fake-write/time sequence harness          | Pinned outcome/request/state generator |
| ten-artifact transaction entries                 | Atomic generation and rollback closure |

</artifacts_produced>

<verification>
Run both task commands, `npm run lint`, `npm run format:check`, `git diff --check`, and verify no diff in the seven pre-Phase-3 fixtures. No live TCP or hardware write may occur.
</verification>

<success_criteria>

- WRT-01/WRT-02/TRN-03W/ERR-01W each have Python-derived planned evidence.
- Evidence is generated before implementation and public promotion.
- Package remains private and release mode remains fail-closed.
  </success_criteria>

<output>Create `.planning/phases/03-safe-write-parity/03-02-SUMMARY.md` when done.</output>
