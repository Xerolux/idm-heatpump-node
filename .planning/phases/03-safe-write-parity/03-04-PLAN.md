---
phase: 03-safe-write-parity
plan: "04"
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - src/client/idm-modbus-client.ts
  - src/client/internal-create.ts
  - src/client/write-safety.ts
  - test/client/resilience.test.ts
  - test/client/write-execution.test.ts
  - test/client/write-state.test.ts
autonomous: true
requirements: [WRT-01, WRT-02, TRN-03W, ERR-01W]
must_haves:
  truths:
    - "Private/internal async writeRegister and setValue transactions use one whole-operation FIFO and no second lock; the seven public class members remain absent until Plan 03-07."
    - "Internal simulation, synchronous queries, and synchronous resets never acquire the FIFO, connect, or send; reset-vs-in-flight ordering matches pinned Python."
    - "Real writes use exact FC16 plans, client-owned attempts/backoff/reconnect, and commit safety state only after transport success."
    - "Every failed attempt records a bounded redacted write context without value, words, raw cause, response, or endpoint."
    - "Write failures never add unsupported/permanent read state and never create false EEPROM/cyclic success."
  artifacts:
    - path: src/client/idm-modbus-client.ts
      provides: private write engine and transactional execution without public class promotion
    - path: src/client/internal-create.ts
      provides: hidden deterministic write-state seams
    - path: test/client/write-execution.test.ts
      provides: request/retry/FIFO/rollback proof
  key_links:
    - from: src/client/idm-modbus-client.ts
      to: src/client/write-safety.ts
      via: planning and sole post-ack recordSuccessfulWrite call
      pattern: "createWritePlan|recordSuccessfulWrite"
    - from: src/client/idm-modbus-client.ts
      to: src/transport/types.ts
      via: createModbusWriteRequest then narrowed ModbusWriteTransport.write
      pattern: "createModbusWriteRequest|\.write\(request\)"
---

<objective>
Implement the complete write engine behind private/internal controls as one serialized validate-to-commit transaction, while keeping the seven public members absent until generated parity closes.

Purpose: Deliver safe dry-run and fake-transport execution while preserving exact Python retries, diagnostics, and rollback.
Output: Private write controls, internal write state seams, and focused execution/FIFO tests.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@src/client/idm-modbus-client.ts
@src/client/internal-create.ts
@src/client/fifo-gate.ts
@src/client/write-safety.ts
@src/transport/errors.ts
@src/transport/types.ts
@test/support/fake-clock.ts
@test/support/fake-modbus-transport.ts
@test/fixtures/write-behavior.json
@.planning/phases/03-safe-write-parity/03-RESEARCH.md

<interfaces>
The internal test surface mirrors future public semantics: simulation is synchronous and traffic-free; internal writeRegister returns `Promise&lt;void&gt;`; internal setValue returns `Promise&lt;WriteSafetyResult&gt;`; resets/queries are synchronous and traffic-free. Only the two async transactions enter the FIFO. A synchronous reset called while a write awaits acknowledgement executes immediately; the later successful acknowledgement then records a new timestamp/deadline, matching pinned Python. Actual write requests always use holding/FC16 regardless of read-side RegisterDef.registerType. Public declarations and the 22/7 mapping partition remain unchanged through Plan 03-06.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-04-01: Add private write controls and the sole success commit point</name>
  <files>src/client/idm-modbus-client.ts, src/client/internal-create.ts, src/client/write-safety.ts, test/client/write-execution.test.ts, test/client/write-state.test.ts</files>
  <read_first>
    - src/client/idm-modbus-client.ts
    - src/client/internal-create.ts
    - src/client/fifo-gate.ts
    - src/client/write-safety.ts
    - src/transport/types.ts
    - test/support/fake-modbus-transport.ts
    - test/support/fake-clock.ts
  </read_first>
  <action>
Write RED internal/default/no-traffic/request/state tests. Add exact option types in `write-safety.ts`, but do not add the seven methods to the public `IdmModbusClient` class. Extend the existing WeakMap control and `internal-create.ts` with test-only `simulateInternalWrite`, `writeInternalRegister`, `setInternalValue`, `resetInternalWriteThrottle`, `getInternalActiveCyclicWrites`, `getInternalExpiredCyclicWrites`, and `resetInternalCyclicWriteState`. These closures call private client helpers and never appear in `src/client/index.ts`, `src/index.ts`, or emitted declarations. Internal simulation defaults dryRun true and never acquires/connects/sends. Internal setValue defaults false; its dry-run path enters the async transaction FIFO but returns before ensure/connect. Internal writeRegister and real setValue acquire the existing `FifoGate` once, perform lookup/validation/EEPROM check inside it, narrow the connected transport to `ModbusWriteTransport`, create the frozen holding/FC16 request, execute all attempts, then call `WriteSafetyState.recordSuccessfulWrite` exactly once after resolved acknowledgement. Add hidden `InternalWriteStateSeed`/snapshot projection and `seedInternalWriteState` through the same boundary.

Test exact one-/two-word requests, all three dryRun defaults, no-model/model/custom behavior, per/global resets, state only after success, failed refresh preserving prior deadline, two concurrent first EEPROM writes (one FC16 then one throttle rejection), mixed read/write `maxActiveRequests === 1`, and queue continuation after rejection. Add explicit reset-vs-in-flight cases: seed prior state, pause a fake write after it enters the transport, call the synchronous reset and observe immediate clearing without waiting for the FIFO, release the write, and observe that successful completion records the new EEPROM timestamp/cyclic deadline. Assert the public class declaration and API mapping remain the exact Phase-2 22 implemented/seven omitted partition.
</action>
<acceptance_criteria> - Internal controls exercise future signatures/defaults while public declarations/mappings remain unchanged and omit all seven methods. - Dry-run/rejected calls produce zero connect/write events and unchanged safety state. - Accepted requests preserve unit/address/count/words and always use FC16. - Concurrent async operations prove validation through commit is one FIFO acquisition. - Synchronous reset is immediate during an in-flight write, and later success repopulates only the just-written register state.
</acceptance_criteria>
<verify>
<automated>npm test -- test/client/write-execution.test.ts test/client/write-state.test.ts &amp;&amp; npm run typecheck</automated>
</verify>
<done>The full write engine exists behind private controls with a single acknowledged-success state boundary and no premature public API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-04-02: Match write retry, reconnect, rollback, and diagnostics</name>
  <files>src/client/idm-modbus-client.ts, test/client/resilience.test.ts, test/client/write-execution.test.ts</files>
  <read_first>
    - src/client/idm-modbus-client.ts
    - src/client/diagnostics.ts
    - src/transport/errors.ts
    - test/client/resilience.test.ts
    - test/client/write-execution.test.ts
    - test/fixtures/write-behavior.json
  </read_first>
  <action>
Implement private `#retryWriteLocked` and write-context recording without calling the read retry helper. Timeout/disconnected/socket/no_response set suspect, close/reconnect, and delay `0.5 * 2 ** attemptIndex`; generic modbus and invalid_response retry on the same connection; ordinary write Code 2 is generic modbus and never mutates unsupported/permanent sets; an already-structured `IllegalAddressError` stops immediately but still does not mutate read state. Initial ensure/connect failure remains outside the attempt loop. Record `operation:"write"`, exact address/count, `registerType:"holding"`, closed kind, redacted bounded message, one-based attempt, and no requested value/words/raw cause/payload. On later success clear suspect but preserve latest error context. Exhaustion and every failure class leave EEPROM/cyclic maps unchanged except preserving pre-existing state.
  </action>
  <acceptance_criteria>
    - Three attempts delay exactly 0.5 then 1.0 seconds with cumulative fake clock 0.5/1.5.
    - Reconnect classes use fresh lifecycle; generic Modbus/invalid acknowledgement do not reconnect.
    - Code 2 write traces retry and unsupported/permanent read state remains unchanged.
    - Serialized errors/contexts contain no endpoint, value, words, response, cause, or adapter object.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/client/write-execution.test.ts test/client/resilience.test.ts -t "write|retry|Code 2|rollback|FIFO|redact" &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>ERR-01W client semantics match Python without any false success or read-state pollution.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts This Phase Produces

| New symbol/path                                                     | Purpose                                                        |
| ------------------------------------------------------------------- | -------------------------------------------------------------- |
| `SimulateWriteOptions` / `WriteRegisterOptions` / `SetValueOptions` | Future public shapes exercised internally                      |
| `simulateInternalWrite`                                             | Private traffic-free planning control                          |
| `writeInternalRegister`                                             | Private FIFO-backed register-write control                     |
| `setInternalValue`                                                  | Private FIFO-backed key/dry-run control                        |
| four `*Internal*` throttle/cyclic query/reset controls              | Private synchronous Python-equivalent state controls           |
| `InternalWriteStateSeed` / internal write snapshot                  | Hidden deterministic test state                                |
| `seedInternalWriteState`                                            | Internal state seeding helper                                  |
| `test/client/write-execution.test.ts`                               | FIFO/retry/request/reset-order/rollback suite                  |
| public `IdmModbusClient` write methods                              | Explicitly absent here; attached atomically only in Plan 03-07 |

</artifacts_produced>

<verification>
Run both task commands plus all existing client tests, lint, format, typecheck, and diff check. No root export/promotion, provider call, register edit, live socket, or hardware validation.
</verification>

<success_criteria>

- All seven future behaviors are proven through private controls while the public class still omits them.
- WRT-01/WRT-02/TRN-03W/ERR-01W execution semantics have focused proof.
- Failed writes cannot create success state or unsupported read state.
  </success_criteria>

<output>Create `.planning/phases/03-safe-write-parity/03-04-SUMMARY.md` when done.</output>
