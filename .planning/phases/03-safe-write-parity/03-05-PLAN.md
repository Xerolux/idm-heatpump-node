---
phase: 03-safe-write-parity
plan: "05"
type: execute
wave: 5
depends_on: [03-01, 03-04]
files_modified:
  - src/transport/errors.ts
  - src/transport/modbus-serial-adapter.ts
  - src/transport/types.ts
  - test/client/batching.test.ts
  - test/client/lifecycle.test.ts
  - test/client/reads.test.ts
  - test/client/resilience.test.ts
  - test/client/write-execution.test.ts
  - test/parity/transport-contract.test.ts
  - test/support/fake-modbus-transport.ts
  - test/transport/modbus-serial-adapter.test.ts
autonomous: true
requirements: [TRN-03W, ERR-01W, WRT-02]
must_haves:
  truths:
    - "The only real provider mapping calls modbus-serial writeRegisters for every FC16 request, including one word."
    - "The adapter resolves only after exact integer address/length echo validation."
    - "Ordinary provider Modbus Code 2 is operation-aware: illegal_address for reads but generic modbus for writes."
    - "Adapter retries remain zero and every test fully mocks the provider without TCP or hardware."
    - "The temporary ModbusWriteTransport refinement merges into ModbusTransport only after the real adapter and every repository implementer are updated in this plan."
  artifacts:
    - path: src/transport/modbus-serial-adapter.ts
      provides: audited FC16 provider mapping and acknowledgement validation
    - path: src/transport/errors.ts
      provides: operation-aware structured Modbus normalization
    - path: test/transport/modbus-serial-adapter.test.ts
      provides: mocked write/ack/error proof
  key_links:
    - from: src/transport/modbus-serial-adapter.ts
      to: modbus-serial@8.0.25
      via: setID plus writeRegisters(address, copied words)
      pattern: "writeRegisters"
    - from: src/transport/modbus-serial-adapter.ts
      to: src/transport/errors.ts
      via: normalize operation write before Code-2 classification
      pattern: "write.*modbus"
---

<objective>
Bind exact client FC16 requests to the audited real provider without moving policy, retries, or state into the adapter.

Purpose: Close the production transport boundary and reject malformed successes before client safety state can commit.
Output: Operation-aware errors, writeRegisters mapping, exact acknowledgement validation, and provider-mock tests.
</objective>

<execution_context>
@/mnt/c/Users/basti/.codex/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/basti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@package.json
@src/transport/errors.ts
@src/transport/modbus-serial-adapter.ts
@src/transport/types.ts
@test/transport/modbus-serial-adapter.test.ts
@.planning/phases/03-safe-write-parity/03-RESEARCH.md

<interfaces>
`modbus-serial@8.0.25` exposes `writeRegisters(address, values)` returning `{address,length}`. Normal FC16 response echoes start address and quantity. Provider details and acknowledgement types remain internal. Plan 03-01's temporary `ModbusWriteTransport` refinement is used until this plan updates the real adapter and every local implementer; only then is `write` merged into the public `ModbusTransport` interface.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-05-01: Map every write to mocked provider FC16 and validate exact echo</name>
  <files>src/transport/modbus-serial-adapter.ts, src/transport/types.ts, test/client/batching.test.ts, test/client/lifecycle.test.ts, test/client/reads.test.ts, test/client/resilience.test.ts, test/parity/transport-contract.test.ts, test/support/fake-modbus-transport.ts, test/transport/modbus-serial-adapter.test.ts</files>
  <read_first>
    - src/transport/modbus-serial-adapter.ts
    - src/transport/types.ts
    - test/transport/modbus-serial-adapter.test.ts
    - every file returned by `rg -l "implements ModbusTransport|: ModbusTransport" src test`
    - package.json
  </read_first>
  <action>
Write RED provider tests. Extend `ModbusSerialClientBoundary` with `writeRegisters(address, values): Promise&lt;unknown&gt;`, add write events/outcomes to the mock, and implement `ModbusSerialTransport.write` under the temporary refinement. Revalidate the request factory, call `setID`, pass a fresh mutable copy of frozen words to `writeRegisters`, and accept only a non-array object whose integer `address === request.address` and integer `length === request.count`. Missing/wrong/fractional/negative/out-of-range echoes normalize to `invalid_response`; extra object fields do not establish success. Test one-word and `[0,16938]` two-word order. After the real adapter is green, merge `write(request: ModbusWriteRequest): Promise&lt;void&gt;` into `ModbusTransport`, remove/alias the temporary refinement, and update every repository implementer found by the stated `rg` audit in the same task. Read-only local doubles receive deterministic never-used write stubs; the fake and real adapter retain actual scripted behavior. Run all affected read suites to prove no behavior drift. Preserve read temporary-timeout behavior, lifecycle idempotence, dependency pin, and zero adapter retries.
  </action>
  <acceptance_criteria>
    - One provider call occurs per client attempt and always uses `writeRegisters`, never `writeRegister`.
    - Unit, address, and word order match the frozen request exactly.
    - Only an exact address/length echo resolves.
    - No provider acknowledgement type leaks through `ModbusTransport` or declarations.
    - The full repository typechecks with no omitted ModbusTransport implementer.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/transport/modbus-serial-adapter.test.ts test/parity/transport-contract.test.ts test/client/batching.test.ts test/client/lifecycle.test.ts test/client/reads.test.ts test/client/resilience.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>The real adapter has one strict mocked FC16 path with no policy or retry duplication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-05-02: Separate write Code 2 and prove malformed-success rollback</name>
  <files>src/transport/errors.ts, src/transport/modbus-serial-adapter.ts, test/client/write-execution.test.ts, test/transport/modbus-serial-adapter.test.ts</files>
  <read_first>
    - src/transport/errors.ts
    - src/transport/modbus-serial-adapter.ts
    - src/client/idm-modbus-client.ts
    - test/client/errors.test.ts
    - test/client/write-execution.test.ts
    - test/transport/modbus-serial-adapter.test.ts
  </read_first>
  <action>
Make adapter normalization operation-aware with a closed `AdapterOperation` write arm or an explicit `createWriteModbusFailure`: numeric Code 2 from `read` remains `IllegalAddressError`; the same structured numeric provider evidence from `write` becomes `NormalizedTransportFailureKind.MODBUS`. Never use messages or numeric strings. Provider write errors retain existing endpoint redaction and omit raw causes/payloads/words. Through a real-adapter/client test, prove wrong/missing echoes retry as invalid_response on the same connection and never record EEPROM/cyclic state; provider Code 2 retries as modbus and never changes unsupported/permanent sets; exhaustion retains the last exact write context.
  </action>
  <acceptance_criteria>
    - Read-side Code-2 tests remain unchanged and green.
    - Write-side Code 2 retries under maxRetries and does not become illegal_address.
    - Every malformed echo prevents state commit, including after a previous cyclic deadline.
    - Error objects and serialized context contain no words/value/provider result/cause/endpoint.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/transport/modbus-serial-adapter.test.ts test/client/write-execution.test.ts -t "malformed|Code 2|unsupported|state|redact"</automated>
  </verify>
  <done>The production boundary preserves Python's write-specific Code-2 behavior and cannot falsely acknowledge a write.</done>
</task>

</tasks>

<artifacts_produced>

## Artifacts This Phase Produces

| New symbol/path                                                 | Purpose                          |
| --------------------------------------------------------------- | -------------------------------- |
| `ModbusSerialClientBoundary.writeRegisters`                     | Mockable provider FC16 seam      |
| `ModbusSerialTransport.write`                                   | Production FC16 implementation   |
| write acknowledgement validator                                 | Exact address/count success gate |
| write arm in `AdapterOperation` / operation-aware Modbus helper | Read/write Code-2 separation     |
| provider write events/outcomes in adapter tests                 | No-network provider evidence     |

</artifacts_produced>

<verification>
Run both task commands, all transport/client tests, lint, format, typecheck, `npm ls modbus-serial --depth=0`, `npm audit --omit=dev`, and diff check. No dependency/lock change or live connection.
</verification>

<success_criteria>

- TRN-03W has exact production provider mapping.
- ERR-01W rejects malformed success and preserves operation-specific Code 2.
- Runtime dependency remains exactly modbus-serial 8.0.25 with no provider retry.
  </success_criteria>

<output>Create `.planning/phases/03-safe-write-parity/03-05-SUMMARY.md` when done.</output>
